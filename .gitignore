# ============================================================
# .gitignore —— 发布到远端前请保留这些忽略规则
# 【作用】把私密配置、构建产物和本地工具配置挡在仓库之外
# ============================================================

# 1) 密钥配置（真实密钥在 miniprogram/config.js，提交到仓库的是 config.example.js）
miniprogram/config.js
# 云函数目录内的真实配置（各目录下的 config.example.js 作为模板提交）
cloudfunctions/*/config.js

# 2) 依赖与构建产物
node_modules/
miniprogram/miniprogram_npm/

# 3) 微信开发者工具本地配置
**/project.private.config.json

# 4) Claude Code 本地配置（不含共享设置）
.claude/

# 5) 系统文件
.DS_Store
Thumbs.db
Desktop.ini
tags

# 6) 云函数部署压缩包（本地打包产物，源码以 cloudfunctions/*/ 目录为准）
cloudfunctions/**/*.zip

# 7) 微信开发者工具诊断临时文件
miniprogram/.diag_tmp.js

# 8) 本地部署备忘（含真实群 chat_id / 机器人 ID，仅供本人本地部署对照，勿上传）
飞书部署-环境变量配置卡.md

# 9) 使用手册的 Word 导出件（正文以「使用手册.md」为准，需要 Word 版请自行导出）
#    忽略的两个理由：① 二进制产物无法 diff / review，不是这个仓库的惯例
#    （仓库里的二进制只有 pages/images/ 下小程序真正需要的图片）；
#    ② Word 会把最后修改人的 Office 显示名写进文档元数据
#    （docProps/core.xml 的 <cp:lastModifiedBy>），推到公开仓库等于公开本机用户名。
使用手册.docx
