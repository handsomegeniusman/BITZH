# 版本更新说明

## 2.1.10

1. **推文详情的猫咪话题换成「圆头像胶囊」**：话题名命中某只猫（真实名 / 别名 / 曾用名 / 昵称，独立词匹配）时，**那一格**胶囊换成「该猫圆形缩略图 + `#话题原文`」的款式，点进猫详情；**没命中猫名的话题样式与行为完全不变**（仍是粉色文字胶囊，点击搜同话题推文）。作者写的标签不会被合并或删除 —— 同一只猫被两个话题同时提到时（`#希特勒` 加它的别名 `#小希`），**两格各带各的头像**。
2. **位置与顺序严格不动**：话题行的格数与顺序完全由作者写的标签决定，命中与否只改「这一格长什么样」。先按"全是普通话题"渲染一版，查询回来再把猫名那几格换掉 —— 两种胶囊文字与位置一致，只有底色和左侧头像变，所以看不到整行跳一下；查询失败时保持前一版（整行按普通话题渲染，不出现空头像、也不整行消失）。
3. **一次查询喂给四处**：话题行头像、点话题跳猫、长按话题进猫编辑页、长按用的 `_id`，全部取自 `markCatTopics` 的同一次 `BITZH` 查询和同一个 `aliasContains`。不新增任何网络请求。这样「胶囊上有猫头像」和「点它 / 长按能到那只猫」永远不会互相矛盾（此前正是这种"两处各判一次"导致过「胶囊有 🐱、长按却没反应」）。
4. **命中判断提成纯函数** `catForm.matchCatsByTopics(topics, cats)`（不碰 db、不碰 setData），因为它抽出来之前无法单测。它同时给出 `topicCats`（话题 → 那只**猫对象本身**，头像要用）和 `catTopicMap`（话题 → `_id`，跳转要用），**两者同口径有断言钉住** —— 一旦不一致就会出现「胶囊上有猫头像、点进去找不到那只猫」。
5. **新增 `tests/catForm.test.js`（37 项）**：独立词边界（「肥猪」不得命中「肥猪头」）+ `topicCats` 与 `catTopicMap` 的一致性不变量 + 别名 / 曾用名 / 昵称命中 + 去重与排序（含"数据库倒序返回时组内顺序也倒"这条反证，用来排除巧合）+ 脏数据兜底 + 纯函数不改动输入。这是**仓库里第一个测 `miniprogram/utils/` 的脚本** —— `catForm.js` 不带 `wx`、require 链上也没有 `config.js`（有一条断言钉住这点，免得日后有人在它顶上加一句 `require('../../config.js')` 把单测变成"只在配好的本机能跑"）。
6. **圆头像沿用既有降级链**：话题行的头像和其余 6 个猫咪列表页走同一套「`.png` → `0.jpg` → 占位图」（`pageUtil.onImgError`），头像底色留了浅粉，图没加载出来时也是一块圆而不是空白。降级标志挂在**话题格**上而不是猫对象上，所以同一只猫出现在两格时两格互不干扰。
7. **建飞书群时能直接看到本群 `chat_id`**：以往只能在 `feishuCallback` 日志里看，但控制台只显示请求入参和响应状态、**看不到运行日志**，配 `FEISHU_APPLY_CHAT_ID` 时会卡住。现在在群里 @ 一次机器人（随便发句话），机器人回的那条「⚠️ 未识别 ···」末尾会带上本群 chat_id，不必翻日志。
8. 测试合计 **385 项**（6 个零依赖 Node 脚本）。

## 2.1.9

1. **发布权限改为「申请-审批制」**：上一版把发布入口收成"仅管理员可见"后，普通用户永远发不了内容。现在已注册用户可在「我的」页点「申请发送帖子」，管理员在小程序「管理员管理」页或飞书申请群里批准，**批准后永久有效**，该用户拿回底部「+」和评论权。规则：**一天只能申请一次**（靠 `PostApply` 的 `_id = userId_北京时间日期` 主键冲突做原子去重）、**申请不需要填理由**、**只给已注册用户**、**黑名单用户不能申请也不能被批**。
2. **新增 `canPost` 字段（`Feeder`）** + 统一的判断函数 `db.canPublish()`：4 个入口（底部加号 / 小猫书浮动按钮 / 发帖页守卫 / 评论输入栏）此前各写一遍 `isAdministrator`，现在只认这一个答案，避免"加号没了但评论还能发"这种漏一处才发现的 bug。修掉一个遗留问题：`resetUserState()` 未清 `state.canPost` 会让切号后残留上一次的已获批状态。
3. **评论区改为「能看不能发」**：评论列表不再有任何权限判断（所有人都能看全部评论），只有**发送**需要发布权限。已注册但未获批的人看到的是**灰掉的输入框 +「还没有发布权限，点「我的」申请」**，而不是被藏掉整栏 —— 藏掉会被误认为"这里本来就不能评论"。未注册仍是「注册后再评论吧」。
4. **新增云函数 `postApply`**（用户提交申请，身份由 `ctx.mpserverless.user.getInfo()` **服务端派生**，不设客户端兜底）+ **`PostApply` 集合**（新建）。
5. **审批的唯一写入者在 `adminManage`**：小程序和飞书两条入口都调同一个 `applyDecision()`。**拒绝绝不碰 `Feeder`** —— 本期明确"不做撤销"，若让拒绝去写 `canPost:false`，一条误发的「拒绝」（在群里是个很短很泛的词）就能抹掉已获批者的权限。授权幂等：已批过的不覆盖 `canPostTime`/`canPostBy`，保住"第一次是谁批的"这份审计记录。
6. **飞书新命令「同意」/「拒绝」（单开一个群）**：`secCheck` 推送目标参数化（`target: 'apply'`）并**按群分桶**做限流/去重（不分桶时申请卡片会吃掉主群的推送配额，把违规告警静默丢掉）。命令词极短极泛，故 `resolveAction` **强制要求推送场景是【发布申请】**，并新增**行首锚定**的 `extractApplicantId` —— 不能复用 `extractOpenid`（它会匹配「被举报人ID：」，在举报推送下打「同意」就会把**被举报人**解析出来并授予发布权）。`verb` 也用 `grantPost`/`denyPost` 而非 `reject`（那个词已被「拉黑用户」占用）。
7. **飞书通道鉴权 `FEISHU_INTERNAL_SECRET`**（fail-closed）：没配 = 这条通道哑掉并明确提示，而不是"谁都能批"。`decidePostApply` 刻意放在管理员校验之外（那条路上没有终端用户身份），第一件事就是验内部密钥。
8. **新增一次性回填 `backfillCanPost`**：把**已经发过帖（仅未下架、非官方）或评论过（未删除）**的老用户批量标为已获批，**跳过黑名单**。`dryRun` 默认 `true`；只写 `Feeder.canPost`，绝不写 `Page`/`Comment`；报告带计数自检 `inconsistent`。🔴 **这一步必须早于评论门槛上线**，否则门槛上线的一瞬间所有现存评论者一起失去评论能力。
9. **`moderate` 头注释写死"刻意不放发布权授予"**：它是契约上任何人都能调用的函数（前端直接 invoke、无鉴权无密码），往它里面加"批准发布"等于新增一个客户端可直接调用的**自助提权接口**。它今天已能被任意调用去封禁别人（是灾难，但是"破坏"），而自授利益是**性质不同**的另一回事。
10. **测试**：新增 `tests/postApply.test.js`（35 项）、`tests/backfillCanPost.test.js`（52 项）；`tests/adminManage.test.js` 扩到 171 项（含审批正/反例、黑名单一票否决、越权负例、`beijingDayKey` 边界）；`tests/feishuCommands.test.js` 扩到 78 项（含场景守卫安全负例）。合计 **348 项全过**。顺带修掉了测试桩的一个假绿灯：`updateMany` 只记调用不落盘，会让"幂等不覆盖 `canPostTime`"这类断言空过。
11. **已知取舍（本期接受，记在 README 第七.5 节）**：评论门槛是**前端 UX 闸门不是安全边界**（评论写入仍在客户端）；`PostApply` 客户端可读；`Feeder.canPost` 客户端可写（用户已知悉）。真正的收口都要先把注册/评论写入搬进云函数，是另一个工作量级。

## 2.1.8

1. **修复注册页「点头像没任何反应」**：上一版为头像加隐私前置拦截时，把 `<button open-type="chooseAvatar">` 改成了 `bindtap` + `wx.chooseAvatar`。微信头像**只能**由官方 open-type 按钮原生触发，程序化调用会静默失效（点击无反应、不报错、不弹窗），把用户卡死在注册。已改回官方写法；`bindtap` 只保留 `wx.requirePrivacyAuthorize` 做隐私授权前置（用户拒绝过隐私指引后原生入口会失效，这才是真正需要补的一环）。
2. **头像改为选填 + 昵称必填**：未选头像不再拦截提交，直接存微信默认头像地址（与用户管理页的默认头像同图）；昵称仍是唯一必填项。头像接口在部分机型 / 隐私未授权时不可靠，注册不再依赖它。
3. 连带修正：改名时仅当原来真有 COS 头像才删旧头像文件；未上传头像的用户，其历史推文 `authorImg` 存默认头像地址，避免头像 404 回退占位图。

## 2.1.7

1. **举报须真实用户（防"不存在的人"恶意举报）**：举报页未注册直接引导注册；`utils/report.js` 查 `Feeder` 兜底；云函数 `moderate.takedown` 服务端校验举报人在 Feeder，绕过前端也挡；举报 / 审核 / 申诉三路飞书推送都带昵称（举报人 / 被举报人 / 用户），管理员一眼认出是谁。
2. **点赞须已注册**：首页 / 分享详情页点赞加 `isFeeder` 守卫，游客引导去注册（发帖 / 评论 / 编辑 / 删除此前已校验，全站写库操作至此全部有权限门）。
3. **飞书命令扩展 + 稳定性**：新增「封禁举报人」「解封举报人」「全部解封」「拉黑用户」；ID 兼容 24 位 hex 空间用户 ID；重复指令 30s 内容去重 + `opId` 持久化幂等；`feishuCallback` await 云函数调用，解决命令"首次不生效、二次才执行"。
4. **联动清理复核中心待办**：飞书封禁 / 解封 / 下架 / 恢复后，`moderate` 自动把相关「待复核 / 举报 / 申诉」pending 待办标记已处理，复核中心不再残留旧待办。
5. **封禁分享拦截**：被封禁的帖子 / 用户内容分享出去不再能点开（仅「用户管理」后台入口放行给管理员）。
6. **正文+话题合并**：`content-editor` 组件改为小红书式两行布局（标题+正文，无小标题），编辑推文页 / 发布推文页接入，存储格式向后兼容。
7. **全站隐私拦截**：`privacy-popup` + `privacy.js` 守卫，覆盖 10 个页面的选图 / 剪贴板 / 头像等隐私入口，弹窗征得同意后才放行。
8. **改名撞同名硬拦截（防 COS 图片互相覆盖）**：编辑推文 / 编辑猫咪时若**真正改名**后撞已有同名 → 硬拦截改回。原因：照片在 COS 里按「标题/名字+序号」命名，同名内容共享图片 key 会互相覆盖（静默数据损坏）；新增 / 回收站恢复的重名拦截此前已有。
9. **测试**：`feishuCommands.test.js` 51 项、`moderateActions.test.js` 12 项（含联动清待办 5 断言）全过。

## 2.1.6

1. 新增内容安全闭环（防封禁）：云函数 `secCheck`（本地敏感词预检 + 微信 msgSecCheck v2 + 企业微信群机器人推送），发帖/编辑/评论/注册昵称在写库前接入审核。
2. 新增举报机制：推文详情/评论长按可举报，写 `Report` 集合并推送企业微信；同用户对同目标 24h 限举报 1 次。
3. 新增误封申诉通道：`appeal` 页 + 全屏 `banned` 封禁页（黑名单用户打开任意 tab 被 reLaunch 清退），管理员邮箱兜底。
4. 新增管理员「复核中心」（`reviewCenter`）：处理举报（下架/封禁/忽略）、申诉（解封/驳回）、疑似内容（标记处理），入口在管理后台「内容安全」。
5. 黑名单清退升级：封禁用户 → 软删其全部推文/评论（打 `hidden`/`deleted` 标志，不物理删、取证留存）+ 禁止查看任何页面；列表统一过滤 `hidden`/`deleted`。
6. 评论删除由物理删除改为软删除（`softDeleteComment`）；新增 `softDeletePage`/`filterHidden`/`banUser`/`unbanUser` 等 `utils/db.js` 方法。
7. 新增配置 `adminEmail`（封禁页/申诉页展示），与云函数环境变量 `ADMIN_EMAIL` 一致。

## 2.1.5

1. 主题刷新：主粉色改为 #FF405E（选中 tab / 按钮 / 徽标等），浅粉 #FFE5EA 作衬底，按压态 #E63A57；功能色按用途区分——设封面提示用黄（#f7a13a）、删除/警示用红（#fa5151），不再强行统一粉色，避免视觉疲劳。
2. 底部导航重设计为小红书式自定义 tabBar：查猫 / 小猫书 / 中间凸起加号（发布新帖）/ 关于 / 我的；发布入口统一到中间加号（已注册跳发帖页、未注册弹注册），移除首页浮动加号。4 个 tab 页 onShow 同步选中态并加底部占位。
3. 搜索支持空格/多关键词分词（查猫页与首页推文搜索）；提交限频统一为「校验之后」约定，失败路径不消耗限频。
4. 话题编辑器修复"添加"无效点击；猫详情关联话题支持多选、展示同标题文章数量与热度。
5. 新增自定义 tabBar 回归测试（test/tabbar.test.js），全量测试通过。
6. 细节：addBooklet 设封面提示钮改深橙 #FF8A2A（功能色）；猫详情无相关推文时自动展开全部详细信息；修复 topic-editor 收到 null 话题串的告警（绑定层 `|| ''` 兜底）；4 个 tab 页去掉 page 标签选择器（消除自定义 tabBar 页的组件化 lint 误报）。

## 2.1.4

1. 修复转发的 bug.

## 2.1.3

1. 修复搜索的 bug.

## 2.1.2

1. 修复 bug.
2. 修改页面样式.

## 2.1.1

1. 修复 bug.

## 2.1.0

1. 增加添加关系的功能.

## 2.0.2

1. 增加编辑信息功能.
2. 增加加猫功能.

## 2.0.2

1. 更改分类的标号.

## 2.0.1

1. 增加图片懒加载的功能.

## 2.0.0

1. 将静态页面改为动态页面.
