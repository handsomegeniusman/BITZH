/* ============================================================
   内容编辑器（正文 + 话题混写）样式
   - 正文 textarea：小红书式，无下划线、可输入区更高
   - #话题 按钮（正文下方 + 键盘工具栏）：同一粉色描边胶囊（需求 12 统一）
   - 建议列表：绝对定位贴在正文下方（向下弹出，占用图片压缩腾出的空间），可滚动
   - 键盘上方工具栏：正文聚焦时显示，固定在键盘顶边（bottom=kbH 行内），左「#话题」右「完成」
   【Bug-A】.ce-kb-bar 用 position:fixed 贴键盘顶，前提是没有任何祖先带 transform/will-change
   （position:fixed 会被 transform 祖先降级成 absolute）。本组件祖先（.form-card/.page-root）
   均无 transform，页面上若有新增 transform 需留意。
============================================================ */
.ce-wrap {
  padding: 20rpx 0 0; /* 左右留白由页面白卡片提供，这里只留上下 */
  background: transparent;
}

.ce-box {
  position: relative; /* 建议下拉定位基准 */
}

/* 正文 textarea 相对定位容器：建议下拉据此定位在正文下方（文章最后一个字下方） */
.ce-textarea-wrap {
  position: relative;
}

/* 正文 textarea：小红书式——无下划线、可输入区更高。
   auto-height + CSS max-height（行内 ceMaxH）限高后，超出部分在内部滚动（不裁剪），
   保证长文时光标所在行可见（adjust-position=false 不自动滚页，见 layoutSuggest） */
.ce-textarea {
  display: block;
  width: 100%;
  box-sizing: border-box;
  min-height: 200rpx;
  max-height: 9999px; /* 默认不限制；聚焦时由行内 style="max-height:{{ceMaxH}}px" 覆盖 */
  font-size: 31rpx;
  color: #333333;
  line-height: 1.6;
  padding: 8rpx 0;
  margin-bottom: 8rpx;
  background: none;
  border: none;
  overflow-y: auto;
}

.ce-placeholder {
  color: #bbb;
}

/* #话题 小按钮：粉色描边小胶囊（正文聚焦时隐藏，见 wxml wx:if="{{!showToolbar}}"）。
   正文下方与键盘工具栏共用此样式，保证两个「话题」按钮完全同款（需求 12） */
.ce-toolbar {
  display: flex;
  align-items: center;
  margin-top: 20rpx;
}
.ce-topic-btn {
  flex-shrink: 0;
  padding: 8rpx 20rpx;
  font-size: 26rpx;
  color: #FF405E;
  background: #fff0f3;
  border: 1rpx solid #ffd6de;
  border-radius: 999rpx;
  line-height: 1.5;
}
.ce-topic-btn:active {
  background: #ffe0e6;
}

/* 建议列表：绝对定位在正文【下方】（top:100%，即 # 号所在行的下一行开始展示），
   实际高度由 JS 按内容算（suggestH，行内 style）：刚好展示几条，封顶「输入框底 → 键盘顶」
   的剩余空间（suggestMaxH），超出部分内部滚动，底边不越过键盘上方工具栏 */
.ce-suggest {
  position: absolute;
  left: 0;
  right: 0;
  top: 100%;
  z-index: 20;
  margin-top: 8rpx;
  background: #fff;
  border: 1px solid #f0e2d2;
  border-radius: 12rpx;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.08);
  overflow: hidden;
}
/* 建议列表滚动区：高度由行内 suggestH 提供（内容高、封顶 suggestMaxH），内部滚动（Bug-C 已扣工具栏高度） */
.ce-suggest-scroll {
  width: 100%;
  box-sizing: border-box;
}
.ce-suggest-item {
  display: flex;
  align-items: center;
  padding: 18rpx 24rpx;
  border-bottom: 1rpx solid #f7f1e8;
}
.ce-suggest-item:last-child {
  border-bottom: none;
}
.ce-suggest-item:active {
  background: #fff7ee;
}
/* 猫名标识：🐱 猫头 emoji（与其他页面话题一致），标明"这是猫名" */
.ce-suggest-cat {
  margin-right: 10rpx;
  font-size: 28rpx;
  flex-shrink: 0;
}
.ce-suggest-name {
  flex: 1;
  font-size: 28rpx;
  color: #222;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.ce-suggest-empty {
  padding: 24rpx;
  text-align: center;
  font-size: 24rpx;
  color: #999;
}

/* 键盘上方工具栏：正文聚焦时显示，固定在键盘顶边（bottom=kbH 行内），
   左「#话题」（同款粉胶囊）右「完成」。Bug-A：position:fixed 依赖无 transform 祖先 */
.ce-kb-bar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0; /* 由行内 style="bottom: {{kbH}}px" 覆盖 */
  z-index: 30;
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 96rpx;
  padding: 0 40rpx;
  box-sizing: border-box;
  background: #fff;
  border-top: 1rpx solid #f0e2d2;
}
.ce-kb-done {
  font-size: 28rpx;
  color: #333;
  padding: 12rpx 8rpx;
}
