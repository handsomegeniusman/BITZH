# 内容编辑器（content-editor）—— 完整需求清单与接手指南

> 本文件是一份**自包含**的接手指南：新接手的 AI 只需读本文件，即可了解全部需求、现状代码、每个 bug 的根因与修复方向、以及必须逐项通过的验收测试。
>
> 项目：微信小程序「小猫书」（仿小红书）。目录 `BITZH/miniprogram/`。
> 核心改造对象：`components/content-editor/`（正文+话题合并编辑器），由 `pages/addBooklet/`、`pages/editBooklet/` 两页复用。

---

## 0. 背景与数据模型

- `content-editor` 组件把「正文 + 话题」合并成一个输入区，仿小红书：话题以 `#话题` 形式写在正文里。
- 存储模型（**保持不变**，展示端零改动）：
  - `main`：纯正文
  - `relative`：话题规范串，格式 `"#话题 #话题"`（由 `utils/topic.js` 的 `build()` 生成、`parse()` 解析）
- 组件通过 `triggerEvent('change', { main, relative })` 上报，页面 `onContentChange` 写回 `listData.main` / `listData.relative`。
- 组件通过 `triggerEvent('editorfocus' / 'editorblur')` 通知页面聚焦/失焦，页面据此压缩/还原图片区。

**关键工具文件（只读，勿改语义）：**
- `utils/topic.js`：`parse(relative)` → 话题数组；`build(topics)` → `"#话题 #话题"`；`tokenRegex` / `tagFilter`
- `utils/db.js`：`db.find(name, filter, { sort, skip, limit })`
- `utils/catForm.js`：`topicCatFilter(topics)`、`aliasContains(stack, name)`（识别猫名）
- `utils/page.js`：`setField(page, path, value)` = `page.setData({[path]: value})`

---

## 1. 需求总清单（含状态）

状态图例：✅ 已解决 / 🔧 方案已定未实施 / 🐛 已定位根因待修复

| # | 需求 | 状态 |
|---|------|------|
| 1 | 点内容/话题/搜索列表时图片缩小、标题+内容上移 | 🔧 部分完成 |
| 2 | 点话题按钮自动补 `#` | ✅ |
| 3 | 搜索关键词能搜出猫名 | ✅ |
| 4 | 列表随键盘位置自动调整（不被键盘盖住） | 🐛 见 Bug-C |
| 5 | 滑动列表/点列表/编辑未失焦都不还原图片，仅真失焦才还原 | ✅ |
| 6 | 标题+内容当**一个整体**对待（切换不重复缩放） | ✅（60ms 防抖已加） |
| 7 | 话题按钮 & 手动 `#` 都在「正文最后一个字下方」弹列表（非话题按钮下方） | ✅ |
| 8 | 猫名在建议列表里标 🐱 | ✅ |
| 9 | 键盘上方工具栏「话题/完成」，出现时隐藏原 `#话题` 按钮 | ✅ |
| 10 | 图片缩小后与标题框间距收窄 | ✅ |
| 11 | 删掉微信原生绿色「完成」栏，自定义「话题/完成」行吸附键盘上方 | 🔧（`show-confirm-bar` 待加，吸附见 Bug-A） |
| 12 | 两个「话题」按钮风格统一（以粉色 `#话题` 胶囊为准） | 🔧（class 待统一） |
| 13 | 话题出现在**正文结尾**，不是单独一行胶囊 | 🔧（架构改造，见第 3 节） |
| 14 | 自定义「话题/完成」工具栏悬浮位置正确（吸附键盘上方，而非内容居中） | 🐛 见 Bug-A |
| 15 | 搜索列表可正常滑动 | 🐛 见 Bug-B |
| 16 | 搜索列表长度随键盘高度动态决定，不被键盘/工具栏盖住 | 🐛 见 Bug-C |
| 17 | 点标题→图片缩小；点话题→图片**保持缩小**（现在反而放大） | 🐛 见 Bug-D |

---

## 2. 现状代码（关键片段）

### 2.1 组件 JS —— `content-editor.js`（核心，约 470 行）

当前数据模型是「正文 `body` + 话题数组 `topics`」分离式：

```js
data: {
  body: '',            // 正文（唯一事实源）
  topics: [],          // 话题数组（渲染成下方胶囊）
  focusMain: false,    // 瞬态聚焦
  cursor: -1,
  suggestions: [],     // [{name, isCat}]
  showSuggest: false,
  suggestDone: false,
  suggestMaxH: 320,
  kbH: 0,              // 键盘高度（px）
  showToolbar: false,  // 键盘上方工具栏是否显示
},

observers: {
  main: function (val) { this.seedBody(val); },
  relative: function (val) { this.seedTopics(val); },
},
```

键盘高度监听（attached 里）：

```js
this._onKbHeight = (res) => {
  const h = (res && res.height) || 0;
  this._kbH = h;
  if (this.data.kbH !== h) this.setData({ kbH: h });
  if (this.data.showSuggest) this.updateSuggestMaxH();
};
if (wx.onKeyboardHeightChange) wx.onKeyboardHeightChange(this._onKbHeight);
```

关键方法现状（节选，完整见文件）：

```js
// 点「#话题」按钮
appendHash() {
  const cur = String(this.data.body || '');
  if (/(^|[#＃\s　，,；;、|])#$/.test(cur)) return;
  const sep = (cur === '' || /[\s　，,；;、|#＃]$/.test(cur)) ? '' : ' ';
  const body = cur + sep + '#';
  this.setData({ body: body, cursor: body.length });
  if (this._focused) {
    this.setData({ focusMain: true });
  } else {
    const self = this;
    this.setData({ focusMain: false });
    wx.nextTick(function () {
      self.setData({ focusMain: true, cursor: String(self.data.body || '').length });
    });
  }
  this.emitChange();
  clearTimeout(this._suggestTimer);
  this.showAllTopics();
},

// 聚焦/失焦
onBodyFocus() {
  this._focused = true;
  this._btnTapAt = 0;
  this.setData({ showToolbar: true });
  this.triggerEvent('editorfocus');
},
onBodyBlur() {
  this._focused = false;
  if (this._btnTapAt && (Date.now() - this._btnTapAt) < 300) {
    this._btnTapAt = 0;
    return; // 点按钮导致的失焦：不还原、不重聚焦（重聚焦交给 appendHash）
  }
  this._btnTapAt = 0;
  if (this._picking) { /* 点建议兜底，略 */ return; }
  if (this._suggestInteracting) { return; }
  this._suggestInteracting = false;
  this.setData({ focusMain: false, showToolbar: false });
  this.triggerEvent('editorblur');
  this._picking = false;
  this._pickItem = null;
  this.flushKeepSuggest();
},

// 建议下拉最大高度
updateSuggestMaxH() {
  const self = this;
  wx.nextTick(function () {
    self.createSelectorQuery().select('.ce-textarea-wrap').boundingClientRect(function (rect) {
      if (!rect) return;
      const winH = (typeof wx.getWindowInfo === 'function' ? wx.getWindowInfo().windowHeight : 0) || 640;
      const kbH = self._kbH || 0;
      let maxH = winH - kbH - rect.bottom - 12;
      if (maxH < 40) maxH = 40;
      if (maxH > 360) maxH = 360;
      self.setData({ suggestMaxH: Math.round(maxH) });
    }).exec();
  });
},

// 提交兜底：末尾 #话题 剥离成胶囊
flushCore(clearSuggest) { /* 见文件 */ },
emitChange() {
  this.triggerEvent('change', {
    main: String(this.data.body || ''),
    relative: topic.build(this.data.topics),
  });
},
```

### 2.2 组件 WXML —— `content-editor.wxml`

```html
<view class="ce-wrap" catchtap="noop">
  <view class="ce-box">
    <view class="ce-textarea-wrap">
      <textarea
        class="ce-textarea"
        value="{{body}}"
        maxlength="{{maxlength}}"
        placeholder="{{placeholder}}"
        placeholder-class="ce-placeholder"
        auto-height="true"
        hold-keyboard="{{true}}"
        cursor-spacing="120"
        confirm-type="done"
        focus="{{focusMain}}"
        cursor="{{cursor}}"
        bindinput="onBodyInput"
        bindfocus="onBodyFocus"
        bindconfirm="onBodyConfirm"
        bindblur="onBodyBlur"
      ></textarea>

      <view class="ce-suggest" wx:if="{{showSuggest}}">
        <scroll-view class="ce-suggest-scroll" scroll-y="{{true}}" style="height: {{suggestMaxH}}px;">
          <block wx:if="{{suggestions.length}}">
            <view class="ce-suggest-item"
              wx:for="{{suggestions}}" wx:key="name" data-item="{{item.name}}"
              catchtouchstart="onItemTouchStart"
              bindtouchmove="onItemTouchMove"
              bindtouchend="onItemTouchEnd"
              catchtouchcancel="onItemTouchCancel"
              bindtap="tapSuggestion">
              <text wx:if="{{item.isCat}}" class="ce-suggest-cat">🐱</text>
              <text class="ce-suggest-name">{{item.name}}</text>
            </view>
          </block>
          <view wx:elif="{{suggestDone}}" class="ce-suggest-empty">没有匹配，输入任意文字即可当作话题</view>
        </scroll-view>
      </view>
    </view>

    <view class="ce-toolbar" wx:if="{{!showToolbar}}">
      <view class="ce-topic-btn" catchtap="appendHash" catchtouchstart="onBtnTouchStart">#话题</view>
    </view>

    <!-- 话题胶囊（需删除，见需求 13） -->
    <view class="ce-chips" wx:if="{{topics.length}}">
      <view class="ce-chip" wx:for="{{topics}}" wx:key="*this">
        <text class="ce-chip-text">#{{item}}</text>
        <text class="ce-chip-del" catchtap="removeTopic" data-index="{{index}}">×</text>
      </view>
    </view>
  </view>

  <!-- 键盘上方工具栏（Bug-A 的目标） -->
  <view class="ce-kb-bar" wx:if="{{showToolbar}}" style="bottom: {{kbH}}px;">
    <text class="ce-kb-topic" catchtap="appendHash" catchtouchstart="onBtnTouchStart">话题</text>
    <text class="ce-kb-done" catchtap="collapseAll">完成</text>
  </view>
</view>
```

### 2.3 组件 WXSS —— `content-editor.wxss`（关键）

```css
.ce-textarea-wrap { position: relative; }

.ce-suggest {
  position: absolute; left: 0; right: 0; top: 100%;
  z-index: 20; margin-top: 8rpx;
  background: #fff; border: 1px solid #f0e2d2; border-radius: 12rpx;
  box-shadow: 0 8rpx 24rpx rgba(0,0,0,.08); overflow: hidden;
}
.ce-suggest-scroll { width: 100%; box-sizing: border-box; }

.ce-topic-btn {          /* 粉色描边胶囊 —— 需求 12 要统一的目标样式 */
  flex-shrink: 0; padding: 8rpx 20rpx; font-size: 26rpx; color: #FF405E;
  background: #fff0f3; border: 1rpx solid #ffd6de; border-radius: 999rpx; line-height: 1.5;
}

.ce-kb-bar {
  position: fixed; left: 0; right: 0; z-index: 30;
  display: flex; align-items: center; justify-content: space-between;
  height: 96rpx; padding: 0 40rpx; box-sizing: border-box;
  background: #fff; border-top: 1rpx solid #f0e2d2;
}
.ce-kb-topic { font-size: 28rpx; color: #FF405E; padding: 12rpx 8rpx; }
.ce-kb-done  { font-size: 28rpx; color: #333; padding: 12rpx 8rpx; }
```

### 2.4 页面焦点处理（addBooklet.js / editBooklet.js 相同）

```js
// 标题聚焦/失焦、正文聚焦/失焦，分别维护独立标记
onTitleFocus() { this.setData({ titleFocus: true });  this.syncContentFocused(); },
onTitleBlur()  { this.setData({ titleFocus: false }); this.syncContentFocused(); },
onEditorFocus(){ this.setData({ editorFocus: true });  this.syncContentFocused(); },
onEditorBlur() { this.setData({ editorFocus: false }); this.syncContentFocused(); },

// 任一聚焦即压缩图片，都失焦才还原；60ms 防抖解决 blur/focus 先后顺序不定
syncContentFocused() {
  clearTimeout(this._focusSyncTimer);
  this._focusSyncTimer = setTimeout(() => {
    const focused = !!(this.data.titleFocus || this.data.editorFocus);
    if (focused !== this.data.contentFocused) this.setData({ contentFocused: focused });
  }, 60);
},
```

页面 wxml 里，图片区压缩、卡片上移靠 `contentFocused` 驱动：

```html
<image-sorter ... compact="{{contentFocused}}" show-tools="{{!contentFocused}}" show-add="{{!contentFocused}}" />
<view class="form-card {{contentFocused ? 'is-compact' : ''}}">
  <input ... bindfocus="onTitleFocus" bindblur="onTitleBlur" />
  <content-editor ... bind:editorfocus="onEditorFocus" bind:editorblur="onEditorBlur" />
</view>
```

---

## 3. 四个待修复 Bug 详解（需求 14~17）

### Bug-A（需求 14）：自定义「话题/完成」工具栏悬浮位置错误

**现象**：内容较长时，微信原生「完成」栏能正确吸附键盘上方；但自定义 `.ce-kb-bar` 出现在**内容居中**位置，没贴到键盘顶。

**根因分析（按可能性排序，需逐条排查）**：

1. **`position: fixed` 被祖先 `transform` 破坏**（最可疑）。微信里 `position: fixed` 若祖先有 `transform`/`filter`/`will-change`/`perspective`，会退化为相对该祖先定位（等同 absolute），于是工具栏跟着文档流跑，内容长时就跑到中间。排查：`.form-card`、`.page-root`、`image-sorter` 及其 transition（image-sorter.wxss 里 `.is-mv/.is-card/... { transition: width/height ... }` 不含 transform，但 compact 切换时若有别处加了 transform 需确认）。
2. **`kbH` 未及时更新**：`showToolbar` 在 `onBodyFocus` 立即变 true，此时键盘高度事件可能尚未回调，`kbH` 仍为 0，工具栏先渲染在 `bottom: 0`。
3. `adjust-position`（textarea 默认 true）会上推页面，但 fixed 元素不受上推影响；若 kbH 与键盘实际高度不符（如算上了原生 confirm-bar 高度），会出现错位。

**修复方向**：

```css
/* 确保 fixed 定位生效：去掉/规避祖先 transform；必要时把工具栏提升到页面级 */
.ce-kb-bar {
  position: fixed;
  left: 0; right: 0;
  bottom: 0;              /* 由行内 style="bottom: {{kbH}}px" 覆盖 */
  z-index: 30;
  /* 关键：确认没有 transform 祖先 */
}
```

```js
// 键盘高度监听：确保 kbH 稳定写入，并兜底用 getWindowInfo 算一次
this._onKbHeight = (res) => {
  const h = (res && res.height) || 0;
  this._kbH = h;
  this.setData({ kbH: h });
  this.updateSuggestMaxH();
};
```

**验收**：聚焦正文、键盘弹出后，`.ce-kb-bar` 紧贴键盘顶；内容任意长、页面滚动时工具栏始终贴键盘顶，不随内容走。

---

### Bug-B（需求 15）：搜索列表滑动无效

**现象**：建议下拉的 `scroll-view` 无法上下滚动。

**根因（高置信）**：`.ce-suggest-item` 上用了 `catchtouchstart="onItemTouchStart"`。`catch` 会**阻止 touchstart 冒泡**，scroll-view 靠接收子元素冒泡上来的 touch 事件启动滚动，被拦截后收不到触摸，于是滚不动。

```html
<!-- 问题在这：catchtouchstart 拦掉了 scroll-view 的触摸 -->
<view class="ce-suggest-item"
  catchtouchstart="onItemTouchStart"  <!-- ← 改成 bindtouchstart -->
  bindtouchmove="onItemTouchMove"
  bindtouchend="onItemTouchEnd"
  catchtouchcancel="onItemTouchCancel"
  bindtap="tapSuggestion">
```

**修复**：把 `catchtouchstart` 改为 `bindtouchstart`（`onItemTouchStart` 仍会执行、照常记录点选意图，但不再阻断冒泡）。若改后仍滚不动，再排查 `.ce-suggest` 的 `overflow: hidden` 与 `scroll-view` 高度是否有效（见 Bug-C）。

**验收**：列表超过最大高度时能手指上下滚动；点选仍正常（第一次点击即生效）。

---

### Bug-C（需求 16）：列表长度未按键盘高度自适应，被键盘/工具栏盖住

**现象**：列表底边被键盘或工具栏遮挡，露出不了全部内容。

**根因**：`updateSuggestMaxH` 计算 `winH - kbH - rect.bottom - 12`，但：
- 未扣除自定义工具栏 `.ce-kb-bar` 的高度（96rpx ≈ 48px）；
- `rect.bottom` 取的是 `.ce-textarea-wrap` 底部，页面被键盘上推（adjust-position）后该值可能已变，导致算出的可用高度偏大，列表伸出到键盘后面。

**修复方向**：

```js
updateSuggestMaxH() {
  const self = this;
  wx.nextTick(function () {
    self.createSelectorQuery().select('.ce-textarea-wrap').boundingClientRect(function (rect) {
      if (!rect) return;
      const winH = (typeof wx.getWindowInfo === 'function' ? wx.getWindowInfo().windowHeight : 0) || 640;
      const kbH = self._kbH || 0;
      const barH = self.data.showToolbar ? 48 : 0;  // 工具栏高度（px，与 96rpx 对应）
      let maxH = winH - kbH - barH - rect.bottom - 12;
      if (maxH < 40) maxH = 40;
      if (maxH > 360) maxH = 360;
      self.setData({ suggestMaxH: Math.round(maxH) });
    }).exec();
  });
}
```

> 注意：`createSelectorQuery().select('.ce-textarea-wrap')` 在组件内查询，需确认能查到（组件内 selector 默认限定在本组件作用域，通常可行；若查不到则 `.in(this)` 显式指定）。

**验收**：键盘弹出后列表底边不越过键盘顶，也不被工具栏盖住；键盘收起后列表高度恢复。

---

### Bug-D（需求 17）：点标题图片缩小，点话题反而图片放大

**现象**：点标题 → 图片缩小（正确）；点 `#话题` → 图片反而放大（错误，应保持缩小）。

**根因**：点 `#话题` 按钮时存在「标题失焦」与「正文聚焦」的竞态，或正文聚焦失败：

- 场景：焦点在标题（`titleFocus=true`，图片已压缩）→ 点 `#话题` 按钮 →
  1. 标题 input 先 blur → `onTitleBlur` → `titleFocus=false`；
  2. `appendHash` 里用 `focusMain false→true` 脉冲尝试聚焦正文，期望 `onBodyFocus` 设 `editorFocus=true`；
  3. 若步骤 2 的聚焦未真正生效（脉冲时序 / 平台差异），`editorFocus` 一直是 false，60ms 防抖后 `focused = titleFocus || editorFocus = false` → 图片放大。
- `appendHash` 依赖 textarea 的 `bindfocus` 事件链才通知页面 `editorfocus`，链路长、易被时序打断。

**修复方向**：点 `#话题` 按钮时**主动**通知页面压缩图片，不依赖 textarea 聚焦事件链：

```js
appendHash() {
  const cur = String(this.data.body || '');
  if (/(^|[#＃\s　，,；;、|])#$/.test(cur)) return;
  const sep = (cur === '' || /[\s　，,；;、|#＃]$/.test(cur)) ? '' : ' ';
  const body = cur + sep + '#';
  this.setData({ body: body, cursor: body.length });
  // 主动声明"正在输入正文"，让页面立即压缩图片，避免"标题失焦→放大"的竞态
  this.triggerEvent('editorfocus');
  // 聚焦脉冲（保留原有 _focused 判断）
  if (this._focused) {
    this.setData({ focusMain: true });
  } else {
    const self = this;
    this.setData({ focusMain: false });
    wx.nextTick(function () {
      self.setData({ focusMain: true, cursor: String(self.data.body || '').length });
    });
  }
  this.emitChange();
  clearTimeout(this._suggestTimer);
  this.showAllTopics();
},
```

> 同时核对 `doPick`（点选建议后）也保持同样的「主动 editorfocus + 重聚焦」逻辑，避免点选话题后图片也放大。

**验收**：标题聚焦（图片缩小）→ 点 `#话题` → 图片**保持缩小**不放大；正文聚焦 → 点 `#话题` → 同样保持缩小；点选列表话题后图片保持缩小；只有真正失焦（点外部/点完成）才放大还原。

---

## 4. 需求 11 / 12 / 13 的改造方案（已定，尚未实施）

### 需求 11：删原生绿色「完成」栏

```html
<textarea
  ...
  confirm-type="done"
  show-confirm-bar="{{false}}"   <!-- ← 新增，关闭原生绿色"完成"栏 -->
/>
```

### 需求 12：统一两个「话题」按钮为粉色胶囊

把键盘上方工具栏里的「话题」从 `.ce-kb-topic` 换成 `.ce-topic-btn`，删掉 `.ce-kb-topic` 样式：

```html
<view class="ce-kb-bar" wx:if="{{showToolbar}}" style="bottom: {{kbH}}px;">
  <view class="ce-topic-btn" catchtap="appendHash" catchtouchstart="onBtnTouchStart">#话题</view>
  <text class="ce-kb-done" catchtap="collapseAll">完成</text>
</view>
```

### 需求 13（重点，架构改造）：话题写进正文，去掉胶囊行

把「正文 `body` + 话题数组 `topics`」改成「**只有 `body`，正文+`#话题` 混写**」，提交时再从正文解析出 `relative`、剥离出 `main`。

**改造要点：**

1. 删除：`topics` 数据、`addUnique`、`removeTopic`、`extractOnInput`/`commitRun`/`flushCore` 里的「剥离到胶囊」逻辑、wxml 的 `.ce-chips`、wxss 的胶囊样式。
2. 新增两个纯函数（`includeTail` 区分「编辑中」与「提交时」）：

```js
extractTopics(body, includeTail) {
  const re = includeTail
    ? /[#＃]([^#＃\s　，,；;、|]+)/g
    : /[#＃]([^#＃\s　，,；;、|]+)(?=[\s　，,；;、|#＃])/g;
  // 全局匹配 + 去重保序
}
stripTopics(body, includeTail) { /* 同样正则剥掉 #话题 得纯正文 */ }
```

3. `doPick` 点选建议改为「追加到正文结尾」：

```js
doPick(t) {
  const item = String(t == null ? '' : t).trim();
  if (!item) return;
  let body = String(this.data.body || '');
  body = body.replace(/[#＃][^#＃\s　，,；;、]*$/, ''); // 剥掉结尾正在打的 #片段
  const lead = body && !/[\s　]$/.test(body) ? ' ' : '';
  body = body + lead + '#' + item + ' ';              // 追加 #话题（尾空格=已完成）
  this.setData({ body, cursor: body.length, suggestions: [], showSuggest: false, suggestDone: false });
  this.emitChange();
  // 重新聚焦（保持继续输入）
}
```

4. 实时输出 / 提交兜底：

```js
emitChange(includeTail) {
  const body = String(this.data.body || '');
  const topics = this.extractTopics(body, includeTail);
  const main = this.stripTopics(body, includeTail);
  this._lastEmitted = { main: main, relative: topic.build(topics) };
  this.triggerEvent('change', { main: main, relative: topic.build(topics) });
}
flush() {  // 提交前调用：结尾未完成的 #片段 也算话题
  this.emitChange(true);
  this.setData({ suggestions: [], showSuggest: false, suggestDone: false });
}
```

5. 防重灌（`seedBody` 用 `_lastEmitted` 跳过编辑回灌，避免正文跳字/话题位置乱跳）：

```js
seedBody() {
  const main = String(this.data.main == null ? '' : this.data.main);
  const rel  = String(this.data.relative == null ? '' : this.data.relative);
  if (this._lastEmitted && this._lastEmitted.main === main && this._lastEmitted.relative === rel) {
    return; // 编辑回灌：body 已最新
  }
  const body = [main.replace(/\s+$/, ''), rel.replace(/\s+$/, '')]
    .filter(s => s.trim() !== '').join(' ');
  if (body !== this.data.body) this.setData({ body });
}
```

6. `searchSuggestions` 里的「排除已选」`cur = this.data.topics` 改成 `cur = this.extractTopics(this.data.body, true)`。

**待确认点**：话题在正文**中间**（如「今天 `#小猫` 很可爱」）时，编辑中不实时剥离（原样显示），提交时统一提取到 `relative`、`main` 剥成纯正文。存储/展示均正确。

---

## 5. 涉及文件清单

| 文件 | 改动 |
|------|------|
| `components/content-editor/content-editor.wxml` | 删胶囊块；textarea 加 `show-confirm-bar="{{false}}"`；键盘「话题」换 `.ce-topic-btn`；建议项 `catchtouchstart`→`bindtouchstart`（Bug-B） |
| `components/content-editor/content-editor.js` | 需求 13 重构 + Bug-A/C/D 修复（kbH 兜底、updateSuggestMaxH 扣工具栏高、appendHash 主动 editorfocus） |
| `components/content-editor/content-editor.wxss` | 删胶囊样式、`.ce-kb-topic`；`.ce-kb-bar` 定位核对（Bug-A） |
| `pages/addBooklet.js` / `pages/editBooklet.js` | **无需改**（只调 `flush()` + 收 `change`/`editorfocus`/`editorblur`，接口不变） |

---

## 6. 测试与验收清单（接手后必须逐项验证）

> 建议在微信开发者工具 + 真机各过一遍；真机上 iOS 的 focus/blur 时序与工具不一致，重点验证。

### 图片区缩放
- [ ] 点标题 → 图片缩小、卡片上移、间距收窄
- [ ] 点正文 → 图片缩小
- [ ] 点 `#话题` 按钮 → 图片**保持缩小**（不放大，Bug-D）
- [ ] 点选列表话题 → 图片保持缩小
- [ ] 标题↔正文来回切换 → 图片不闪烁、不重复缩放（需求 6）
- [ ] 点页面外部 / 点「完成」 → 图片还原（需求 5）

### 话题输入
- [ ] 点 `#话题` 按钮 → 正文末尾加 `#`、弹键盘、弹列表
- [ ] 手动输入 `#` → 正文末尾弹列表
- [ ] 列表在「正文最后一个字下方」，不在话题按钮下方（需求 7）
- [ ] 点选话题 → 话题出现在**正文结尾**（非胶囊行，需求 13）
- [ ] 手动输入 `#话题` + 回车 → 话题在正文结尾
- [ ] 猫名在列表里带 🐱（需求 8）
- [ ] 提交后 `main`=纯正文、`relative`=`"#话题 #话题"`（展示端 bookletDetail 正常）

### 键盘与工具栏
- [ ] 只有一个「话题/完成」行，无原生绿色「完成」（需求 11）
- [ ] 工具栏紧贴键盘顶（内容任意长也不跑到中间，Bug-A）
- [ ] 键盘上方的「话题」与正文里的「#话题」按钮风格一致（粉色胶囊，需求 12）

### 建议列表
- [ ] 列表可上下滑动（Bug-B）
- [ ] 列表底边不被键盘/工具栏盖住（Bug-C）
- [ ] 键盘收起后列表高度正常

### 数据与回归
- [ ] 草稿恢复 / 上次数据恢复后正文+话题正确回填
- [ ] 正文 2000 字、话题 100 字限长仍生效
- [ ] 发布 / 修改 / 删除全流程不报错

---

## 7. 给接手 AI 的工作指引

1. 先读本文件 + `content-editor.js` / `.wxml` / `.wxss` 全文，建立模型。
2. 按「先小后大」实施：Bug-B（改 1 个属性）→ 需求 11/12（wxml/wxss 小改）→ Bug-D（appendHash 加一行）→ Bug-A/C（定位 + kbH 兜底）→ 需求 13（架构重构）。
3. 每改一处跑对应验收项，不要一次全改完再测。
4. Bug-A 若排查发现是祖先 `transform` 导致，需在页面侧（`.form-card` / `.page-root`）处理，不要把 transform 加到可能影响 fixed 的祖先上。
5. 需求 13 改动大，改前先确认「话题在正文中间」的处理方式（见 4 节待确认点）与用户预期一致。
6. 全部完成后，跑一遍第 6 节清单，逐项勾选并报告结果；发现清单外的新 bug 单独列出。
