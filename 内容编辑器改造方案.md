# 内容编辑器（content-editor）改造方案

> 目标：让 addBooklet / editBooklet 的内容输入区更贴近小红书——话题直接写在正文里，而不是单独一行胶囊。
> 涉及组件：`BITZH/miniprogram/components/content-editor/`

---

## 一、本次要解决的 3 个问题

| # | 问题描述 | 目标 |
|---|---------|------|
| 1 | 输入话题/正文时出现两行：第一行「话题 / 完成」，第二行绿色「完成」 | 删掉绿色「完成」（微信原生的），只保留自定义「话题 / 完成」行，并让它吸附在键盘上方 |
| 2 | 存在两个「话题」按钮且风格不统一：键盘未弹出时的粉色 `#话题` 胶囊、键盘上方的纯文字「话题」 | 统一为粉色 `#话题` 小胶囊（`.ce-topic-btn`） |
| 3 | 成功输入话题（点列表话题 / 手动输入回车等）后，话题出现在**单独一行胶囊** | 话题直接写在**正文结尾**（`#话题` 跟在正文后），不再有胶囊行 |

---

## 二、现状分析

当前编辑器是「**正文 + 话题分离**」模型：

- 正文存在 `data.body`，话题存在 `data.topics`（数组）。
- 输入 `#小猫` 后按 **空格 / 回车 / 失焦**，末尾的 `#小猫` 会被从 `body` **剥走**，塞进 `topics` 数组，渲染成下方一排粉色胶囊（`.ce-chips`）。
- 提交时 `emitChange` 输出 `{ main: 纯正文, relative: "#话题 #话题" }`。

### 关键现状代码

```js
// content-editor.js —— 现状：输入时把末尾 #话题 剥离成胶囊
onBodyInput(e) {
  ...
  const hit = this.extractOnInput(raw);   // 末尾有空格/换行 → 判定话题"已完成"
  if (hit) {
    this.commitRun(hit.body, hit.topics); // 剥离正文 + 话题入 topics 数组
  } else {
    this.setData({ body: raw });
  }
}

// 现状：话题数组 → 胶囊
addUnique(topics, t) { ... topics.push(s); ... }

// 现状：提交时输出
emitChange() {
  this.triggerEvent('change', {
    main: String(this.data.body || ''),      // 纯正文
    relative: topic.build(this.data.topics), // "#话题 #话题"
  });
}
```

```html
<!-- content-editor.wxml —— 现状 -->
<textarea ... confirm-type="done" />   <!-- 未加 show-confirm-bar，原生绿色"完成"仍显示 -->

<!-- 两个话题按钮风格不一 -->
<view class="ce-toolbar" wx:if="{{!showToolbar}}">
  <view class="ce-topic-btn" ...>#话题</view>   <!-- 粉色描边胶囊 -->
</view>
<view class="ce-kb-bar" ...>
  <text class="ce-kb-topic" ...>话题</text>       <!-- 纯文字，无背景边框 -->
</view>

<!-- 话题胶囊单独一行 -->
<view class="ce-chips" wx:if="{{topics.length}}"> ... </view>
```

---

## 三、改造方案

### 问题 1：删除绿色「完成」，完成行吸附键盘

- **绿色「完成」来源**：微信 `textarea` 原生属性 `show-confirm-bar` 默认 `true`，会在键盘上方显示一行原生「完成」按钮。
- **改法**：给 textarea 加 `show-confirm-bar="{{false}}"`，其余零改动。
- 自定义「话题 / 完成」行（`.ce-kb-bar`）**已经是** `position: fixed; bottom: {{kbH}}px`，`kbH` 来自 `wx.onKeyboardHeightChange`，键盘弹出时自动吸附在键盘顶。此步已具备，问题 1 只需关掉原生行。

```html
<textarea
  ...
  confirm-type="done"
  show-confirm-bar="{{false}}"   <!-- ← 新增：关闭原生绿色"完成"栏 -->
/>
```

### 问题 2：两个「话题」按钮统一为粉色胶囊

- **改法**：键盘上方那一行的「话题」按钮，class 从 `.ce-kb-topic` 换成 `.ce-topic-btn`（复用同一胶囊样式），并删除 `.ce-kb-topic` 样式定义。两个按钮即完全同款。

```html
<!-- 键盘上方工具栏：话题按钮复用 .ce-topic-btn -->
<view class="ce-kb-bar" wx:if="{{showToolbar}}" style="bottom: {{kbH}}px;">
  <view class="ce-topic-btn" catchtap="appendHash" catchtouchstart="onBtnTouchStart">#话题</view>
  <text class="ce-kb-done" catchtap="collapseAll">完成</text>
</view>
```

- 关于「粉色按钮点击应：缩图 + 正文结尾加 `#` + 弹列表 + 弹键盘」——该链路**现已具备**：`appendHash()` 加 `#` → `setData focusMain:true` 弹键盘 → `showAllTopics()` 弹列表 → 聚焦触发 `onBodyFocus → triggerEvent('editorfocus')` 让页面缩图。问题 2 主要是**样式统一**，行为无需大改。

### 问题 3（重点）：话题写进正文，去掉胶囊行

这是**架构改动**：把「正文 + 话题分离」改成「话题直接写在正文里」。

#### 数据模型变化

| | 现状 | 改后 |
|---|---|---|
| 编辑器内部 | `body`(纯正文) + `topics`(数组) | **只有 `body`**，正文 + `#话题` 混写 |
| 提交输出 | `main`=body, `relative`=build(topics) | `main`=剥离后的纯正文, `relative`=从 body 解析的 `#话题` |
| 存储格式 | 不变 | **不变**（main=纯正文，relative="#话题 #话题"） |

#### 具体改动

1. **删除**：`topics` 数据、`addUnique`、`removeTopic`、`extractOnInput` / `commitRun` / `flushCore` 里的「剥离到胶囊」逻辑，以及 wxml 的 `.ce-chips`、wxss 的胶囊样式。

2. **新增两个纯函数**（从 body 提取 / 剥离 `#话题`），用 `includeTail` 参数区分「编辑中」和「提交时」：

```js
// 提取 body 里的 #话题（去重保序）
// includeTail=false：只取"已完成"的（#xxx 后跟分隔符），结尾正在打的 #片段不算
// includeTail=true ：连结尾未完成的 #片段也算（提交兜底用）
extractTopics(body, includeTail) {
  const re = includeTail
    ? /[#＃]([^#＃\s　，,；;、|]+)/g
    : /[#＃]([^#＃\s　，,；;、|]+)(?=[\s　，,；;、|#＃])/g;
  // ... 全局匹配 + 去重保序
}
stripTopics(body, includeTail) {
  // 用同样的正则把 #话题 剥掉，剩下纯正文
}
```

3. **点选建议 → 追加到正文结尾**（不再是入数组）：

```js
doPick(t) {
  const item = String(t == null ? '' : t).trim();
  if (!item) return;
  let body = String(this.data.body || '');
  body = body.replace(/[#＃][^#＃\s　，,；;、]*$/, ''); // 剥掉结尾正在打的 #片段
  const lead = body && !/[\s　]$/.test(body) ? ' ' : '';
  body = body + lead + '#' + item + ' ';              // 追加 #话题（末尾空格=已完成）
  this.setData({ body, cursor: body.length, suggestions: [], showSuggest: false, suggestDone: false });
  this.emitChange();
  // ... 重新聚焦（保持继续输入）
}
```

4. **实时输出 / 提交兜底**：

```js
emitChange(includeTail) {
  const body = String(this.data.body || '');
  const topics = this.extractTopics(body, includeTail);
  const main = this.stripTopics(body, includeTail);
  this._lastEmitted = { main: main, relative: topic.build(topics) };  // ← 防重灌的关键
  this.triggerEvent('change', { main: main, relative: topic.build(topics) });
}

flush() {  // 提交前调用：把结尾未完成的 #片段 也算进话题
  this.emitChange(true);
  this.setData({ suggestions: [], showSuggest: false, suggestDone: false });
}
```

5. **防重灌**（新模型最易踩的坑）：`emitChange` 会实时把「剥离后的 main + 话题串」回灌页面，页面又经 `main` / `relative` 属性回灌组件触发 `seedBody`。若 `seedBody` 简单拼接会导致正文跳字 / 话题位置乱跳。用 `_lastEmitted` 标记：编辑回灌（值相同）时跳过，外部真实回填（草稿恢复 / 上次数据恢复）才重新拼正文：

```js
seedBody() {
  const main = String(this.data.main == null ? '' : this.data.main);
  const rel  = String(this.data.relative == null ? '' : this.data.relative);
  if (this._lastEmitted && this._lastEmitted.main === main && this._lastEmitted.relative === rel) {
    return; // 编辑回灌：body 已最新，跳过
  }
  const body = [main.replace(/\s+$/, ''), rel.replace(/\s+$/, '')]
    .filter(s => s.trim() !== '').join(' ');
  if (body !== this.data.body) this.setData({ body: body });
}
```

6. **展示端零改动**：提交后 `main` 仍是纯正文、`relative` 仍是 `"#话题 #话题"`，`bookletDetail` 里「正文 `{{listData.main}}` + 话题胶囊 `{{relativeList}}`」的展示逻辑不变。

---

## 四、涉及文件清单

| 文件 | 改动 |
|------|------|
| `content-editor.wxml` | 删 `.ce-chips` 胶囊块；textarea 加 `show-confirm-bar="{{false}}"`；键盘「话题」换 `.ce-topic-btn` |
| `content-editor.js` | 删 `topics`/胶囊逻辑；加 `extractTopics`/`stripTopics`；改 `doPick`/`emitChange`/`flush`/`seedBody` |
| `content-editor.wxss` | 删 `.ce-chips`/`.ce-chip*`/`.ce-kb-topic` 样式 |
| `addBooklet.js` / `editBooklet.js` | **无需改**（只调 `editor.flush()` 和收 `change` 事件，接口不变） |

---

## 五、待确认点

**话题在正文「中间」时怎么办？** 例如用户打「今天 `#小猫` 很可爱」——话题不在末尾。

- 方案：编辑时**不实时动它**（原样显示），只在**提交时**统一把所有 `#话题` 提取到 `relative`、`main` 剥成纯正文。
- 效果：存储 / 展示都正确，编辑过程中话题显示在中间（与小红书一致，本就允许放中间）。

若认可此方案，即可按上述开始改造；若对问题 3 的存储语义（`main` 是否要含话题）或话题位置有其它想法，再行调整。
