# 已知遗留问题（未修复，需后续关注）

## 1. editCat.js — 原始对象引用共享导致存档损坏 ✅ 已修复

**位置**: [editCat.js:106-107](pages/editCat/editCat.js#L106-L107)

**问题**: `this._originalCat = cat` 和 `this.setData({ cat })` 指向同一个对象。用户编辑表单时 `this.data.cat` 被直接修改，导致 `this._originalCat` 也被同步修改。删除/编辑存档时使用的 `this._originalCat` 已经是修改后的值，而不是 onLoad 时的原始值。

**修复**: `this._originalCat = JSON.parse(JSON.stringify(cat))` 深拷贝（存档与编辑态互不影响）。

---

## 2. editCat.js — 删除猫咪不清除 relatedCats 引用 ✅ 已修复

**位置**: [editCat.js](pages/editCat/editCat.js) `delete()`

**问题**: `delete()` 只从 BITZH 集合删除当前猫，不清理其他猫的 `relatedCats` 字段中对该猫的引用。

**影响**: Cat A 的 `relatedCats` 中有 Cat B，Cat B 被删除后，Cat A 的详情页仍会尝试加载 Cat B（查询不到 → 静默跳过，但逻辑不完整）。

**修复**: 删除前调用 `relation.removeCatRefs(name)` 清理所有 `relatedCats` 中的该猫引用。

---

## 3. editCat.js — 猫改名后 Page 推文关联断裂 ✅ 已修复

**位置**: [editCat.js](pages/editCat/editCat.js) 名称修改逻辑 + [catDetail.js:225-238](pages/catDetail/catDetail.js#L225-L238)

**修复**: 在 `showSubmitConfirm()` 提交成功后、若检测到猫名变更，调用 `updatePageRelative()` 正则替换 Page 集合中 `relative` 字段的旧名→新名，保证改名后 catDetail 推文列表不丢失关联。

---

## 4. catTrash / pageTrash — 回收站无分页

**位置**: [catTrash.js](pages/catTrash/catTrash.js) / [pageTrash.js](pages/pageTrash/pageTrash.js)

**问题**: 回收站一次性加载所有记录，没有分页。当删除记录很多时（几百条），一次性查询开销大、渲染慢。

**影响**: 大量删除记录时加载缓慢，列表渲染性能下降。

**修复建议**: 使用 `db.paginate()` 分页加载，类似 catSearch.js 的实现。

---

## 5. catSearch.js — 搜索不支持空格分词 ✅ 已修复

**位置**: [catSearch.js](pages/catSearch/catSearch.js)

**修复**: 按空格/全角空格分词，多关键词生成 `$and` + 多个 `$regex`，每个词独立模糊匹配。"橘 白" 可匹配 "橘白" 和 "橘猫大白"。

---

## 6. index.js — checkUpdate 全量扫描 ✅ 无需修复

**位置**: [index.js](pages/index/index.js)

**说明**: `checkUpdate()` 已在之前删除投喂功能时一并移除，代码库中不再存在。

---

## 7. addBooklet.js / editBooklet.js — 正文 2000 字校验在客户端，服务端无限制

**位置**: [addBooklet.js:149](pages/addBooklet/addBooklet.js#L149)

**问题**: DB 写入无服务端长度校验，恶意跳过客户端直接调云函数可写入超长内容。

**影响**: 低风险（需要绕过客户端），但数据库设计上应有服务端校验。

**修复建议**: 在云函数/MPServerless 侧增加字段长度校验。

---

## 8. Administrator.js — 管理员操作无撤销机制 ✅ 已修复

**位置**: [Administrator.js](pages/Administrator/Administrator.js)

**修复**: `manage()` 审核开关操作增加确认弹窗（`wx.showModal`），显示当前操作是"开启"还是"关闭"注册/发布审核，防止误操作。

## 9. bookletDetail.js — 删/改评论限频在权限校验之前 ✅ 已修复

**位置**: [bookletDetail.js:231-260](pages/bookletDetail/bookletDetail.js#L231-L260)

**问题**: `deleteComment()` / `modifyComment()` 先 `guard.throttle` 再校验管理员/作者身份。权限不足的点击同样消耗 2 秒限频窗口，用户修正权限后立即重试会被静默拦截。

**修复**: 与 editBooklet 删除逻辑统一 —— 先校验权限（失败直接提示"权限不足"），再限频。权限失败不再消耗限频窗口。

---

## 10. 自定义 tabBar 中间加号不再受"发布审核开关"控制

**位置**: [custom-tab-bar/index.js](miniprogram/custom-tab-bar/index.js) `addPost` / 旧 [index.wxml](miniprogram/pages/index/index.wxml) 浮动加号

**问题**: 改自定义 tabBar 前，首页浮动加号用 `wx:if="{{audit}}"` 控制——管理员关闭发布审核（audit=false）时入口隐藏，且 addBooklet / editBooklet 均不校验 audit。改后中间加号**常驻显示**，已注册用户在 audit=false 时仍可点加号进入发帖页发布，等于绕过审核关闭状态。

**建议**: 保持加号常驻（小红书式视觉），但在 `addPost` 里补一条 `app.globalData.audit === false` 的判断，提示"发布暂未开放"并返回；audit 由 index 页拉取后同步到 `app.globalData.audit`（默认视为关闭）。是否实施由需求方决定。
