# BITZH 小程序 —— 逐页测试清单

## 测试说明
本清单覆盖 17 个前台页面 + 5 个后台管理页 + 11 个工具模块的关键功能、边界条件和异常路径。
请在微信开发者工具中按顺序逐项验证，通过打 ✅。

> **先跑单测再手动过清单**：`node tests/run-all.js`（13 个脚本、842 项，零依赖、不连数据库，1 秒内跑完）。
> 单测只覆盖纯逻辑，所以**全绿不代表线上没问题** —— 数据库、集合权限、云函数部署、界面交互都得靠这份清单。
> 云函数里的 `console.log` **在控制台看不到**（只显示入参和响应状态），排查线上问题要用「云函数 → 调试」传 JSON 看返回值，见 §七.C。
> 本轮（2026-09-16）新增的三块改动集中在 **§六.B** 和 **§七**。

---

## 一、工具模块（utils/）方法覆盖

### db.js — 数据库操作
- [ ] `initUserState()` — 首次调用正常初始化；重复调用返回缓存
- [ ] `findOne(collection, filter)` — 正常查到 / 查不到返回 null
- [ ] `find(collection, filter, opts)` — 分页 limit 生效；空结果返回 []
- [ ] `paginate(collection, filter, opts, existingList)` — 自动去重（用 existingList.length 作 skip）；追加不重复
- [ ] `insertOne(collection, data)` — 插入成功返回新 _id
- [ ] `updateOne(collection, filter, data)` — 更新成功；filter 无匹配时静默
- [ ] `updateMany(collection, filter, data)` — 批量更新正确
- [ ] `deleteOne(collection, filter)` — 删除成功
- [ ] `isBlacklisted()` — 在黑名单返回 true；不在返回 false
- [ ] `getAudit()` — 管理员已设置 audit 字段时正确读取（**全局值**，管理员页用）
- [ ] `getAuditForMe()` — 全局开放 **或** 本人 `enable=true` **或** 本人是管理员（在 `BITZHAdministrator` 名册里）时返回 true（**公共内容**的豁免，见 §D）
- [ ] `getAuditForMyPage()` — 全局开放 **或** 本人**注册过**（有 `Feeder` 记录，不看 `enable`）**或** 本人是管理员时返回 true（「我的」页专用，见 §D）

### guard.js — 前端保险
- [ ] `escapeRegExp(str)` — 特殊字符 `.^$*+?()[{\|` 全部转义
- [ ] `escapeRegExp(null/undefined/数字)` — 兜底返回空字符串（不抛异常）
- [ ] `sanitizeFileName(str, maxLen)` — 去掉路径字符 `/ \ : * ? " < > |`；限长截断
- [ ] `toText(val)` — null/undefined 返回 ''；数字返回字符串
- [ ] `isEmpty(val)` — null/undefined/''/纯空格 返回 true
- [ ] `tooLong(str, max)` — 超长返回 true
- [ ] `throttle(key, ms)` — 同一 key 在 ms 内重复调用返回 false
- [ ] `todayString()` — 返回 yyyy-MM-dd 格式
- [ ] `requireAdmin()` — 非管理员弹出 toast 并返回 false；管理员返回 true

### cos.js — COS 图片
- [ ] `catUrl(name, index, ver)` — 返回带版本号的 .jpg URL
- [ ] `pageJpg(title, index)` — 返回推文图片 key
- [ ] `profileUrl(name)` — 返回头像 .png URL
- [ ] `profilePng(name)` — 返回头像 key
- [ ] `keyFromUrl(url)` — 从完整 URL 反推 COS key
- [ ] `uploadList([{Key, FilePath}])` — 成功上传；部分失败返回成功数
- [ ] `copyList([{from, to}])` — 服务端复制（草稿/存档图迁移）
- [ ] `deleteList([key])` — 删除 COS 文件

### media.js — 选图
- [ ] `chooseImages(page, field, max, compressed, onChange)` — 选 1 张 / 多张 / 达上限拦截
- [ ] 拍摄模式正常
- [ ] 权限拒绝时提示

### imgEditor.js — 图片编辑器
- [ ] 长按拖拽排序：touchStart → touchMove → touchEnd → 顺序正确
- [ ] 单击预览（wx.previewImage）
- [ ] 删除确认弹窗 + "下次不再询问"
- [ ] 设为封面（移到第一位）
- [ ] `remaining(page)` — 正确计算剩余可选张数

### draft.js — 草稿
- [ ] `restore(page, type, id, opts)` — 有草稿时弹窗询问 → 确认恢复 / 放弃
- [ ] `saveNow(page, type, id, fields)` — 正常保存到本地
- [ ] `markDirty(page)` — 标记脏后防抖自动保存
- [ ] `clearDraft(page, type, id)` — 发布成功后清草稿

### page.js — 页面公共
- [ ] `stampThumbs(list, baseUrl)` — 为列表每项添加 `_thumbUrl` / `_thumbJpg`
- [ ] `onImgError(page, e)` — 图片加载失败：.png → 0.jpg → 占位图 逐级回退
- [ ] `showAuthorImg(e)` — 预览作者头像大图
- [ ] `promptRegister(userId)` — 未登录弹出注册引导
- [ ] `editBooklet(e)` — 管理员/作者长按编辑推文

### catForm.js — 猫咪表单
- [ ] `normalizeTextFields(raw)` — null → {} 兜底（已修复：调用侧先判空）
- [ ] 下拉选择器 options 正确
- [ ] `matchCatsByTopics(topics, cats)` — 话题→猫的命中判断（`topicCats` 给头像、`catTopicMap` 给跳转 `_id`，两者同口径的不变量有断言钉住；`cats` 的去重与排序也一并测着）（**已有单测** `node tests/catForm.test.js`，37 项；这里只需在页面上确认第二节 4. bookletDetail 的「猫话题胶囊」那几项）

### relation.js — 关系系统
- [ ] 双向关系建立 / 删除
- [ ] 性别敏感配对（公+母 → 伴侣；同性别 → 兄弟/姐妹）

### trash.js — 回收站
- [ ] `formatTime(date)` — 格式化时间
- [ ] 恢复逻辑：不同集合恢复路径正确

### util.js — 杂项
- [ ] `formatTime(date)` — 与 trash.js 的 formatTime 行为一致

---

## 二、页面功能测试

### 1. index（首页"查猫"）
**数据加载**
- [ ] 首次打开默认显示"在校"tab（不加载其他 tab 数据）
- [ ] 切换到"送养"/"逃学"/"喵星"/"待抓"时懒加载数据
- [ ] 非"在校"tab 下拉触底分页加载更多
- [ ] 从其他页面返回时自动刷新当前 tab
- [ ] 分享链接带 `currentTab` 参数直接跳转到指定 tab

**搜索**
- [ ] 输入猫名 → 300ms 防抖后显示搜索结果
- [ ] 搜索结果按匹配位置排序（靠前优先）
- [ ] 点击结果跳转猫咪详情
- [ ] 输入为空时隐藏搜索结果
- [ ] 搜索关键词 >20 字时拦截提示
- [ ] 失焦 / 点击空白区域时隐藏搜索结果
- [ ] 特殊正则字符（`.^$*+?`）搜索不报错（已转义）
- [ ] 昵称为 null 的旧数据不崩溃（已加 null guard）

**公告 & 黑名单**
- [ ] 公告弹出后点击关闭正常
- [ ] 黑名单用户只显示"请离开"弹窗
- [ ] 弹窗动画正常（popupAnimation 已初始化）

**管理员**
- [ ] 管理员长按猫咪可编辑
- [ ] 管理员点击 Logo 可新增猫咪
- [ ] 非管理员不响应上述操作

**分类导航**
- [ ] 8 个毛色分类 navigator 均正常跳转到 `所有` 页面

### 2. catDetail（猫咪详情）
**照片轮播**
- [ ] 轮播正常滑动，循环播放（circular）
- [ ] 照片加载失败时自动回退到 logo 占位图
- [ ] 点击图片全屏预览
- [ ] `addPhotoNumber` 为 0 / NaN / 负数 时用 logo 占位（已修复）
- [ ] 照片 URL 带 `photoVer` 版本号

**状态显示**
- [ ] 状态标签颜色正确（健康=绿 / 送养=蓝 / 失踪=红 / 离世=灰 / 待抓=黄）
- [ ] 绝育标签颜色正确

**详情展开**
- [ ] 展开/收起详细信息 → 轮播图高度变化 + 动画

**相关猫咪**
- [ ] 显示 relatedCats 列表；关系描述（如"兄弟"）正确
- [ ] 相关猫咪缩略图带版本号
- [ ] 相关猫咪已删除时列表不显示该项

**相关推文**
- [ ] 按猫名模糊匹配推文（relative 字段）
- [ ] 排序切换（拍摄时间/发布时间 × 升序/降序）
- [ ] 上拉触底分页加载
- [ ] 猫名为空时不查询（已修复空正则数据泄露）

**回到页面刷新**
- [ ] 猫被删除后返回 → 占位 + 提示 + 自动返回
- [ ] 猫内容有变化时自动重新渲染
- [ ] 内容没变时不重复渲染（避免轮播闪烁）

**其他**
- [ ] 点赞爱心跳转到推文详情（catDetail 不支持直接点赞）
- [ ] 点击作者头像预览大图
- [ ] 点击作者昵称跳转 someBooklet
- [ ] 管理员长按图片可编辑猫咪

### 3. index（小猫书 — 推文瀑布流，首页）
**数据加载**
- [ ] 首次加载推文瀑布流（左右列分列正确）
- [ ] 下拉刷新
- [ ] 上拉触底加载更多
- [ ] 排序切换（拍摄时间/发布时间/点赞量 × 升序/降序）
- [ ] 公告弹窗显示/关闭
- [ ] 黑名单弹窗显示（popupAnimation 已初始化）

**发布**
- [ ] 有发布权限（管理员 / 申请已获批）的用户：底部出现「+」，点击跳转 addBooklet
- [ ] 无发布权限的用户（未注册 / 已注册未获批）：底部**看不到**「+」（详见第六节）

**交互**
- [ ] 点击推文卡片跳转详情
- [ ] 管理员/作者长按推文可编辑
- [ ] 点赞（数量 +1，可连点多赞）
- [ ] 点击作者头像预览大图
- [ ] 点击作者昵称跳转 someBooklet

### 4. bookletDetail（推文详情）
**内容展示**
- [ ] 轮播图正常滑动（circular 循环）
- [ ] 回收站预览模式（recoverMode）：无照片存档时显示占位提示
- [ ] 标题、正文、话题标签、拍摄时间正确显示
- [ ] 点击话题标签跳转 someBooklet（relative 搜索）

**猫话题胶囊（话题命中猫名时那一格换成圆头像款）**
- [ ] 话题是猫名（真实名 / 别名 / 曾用名 / 昵称任一）→ **那一格**胶囊变成「圆形猫头像 + `#话题原文`」，底色比普通话题胶囊浅
- [ ] 点这一格 → 进对应那只猫的详情页
- [ ] 话题**不是**猫名（如 `#校园日常`）→ 仍是原来的粉色文字胶囊，点击 → 搜同话题推文
- [ ] 一条推文里普通话题和猫话题混排（如 `#校园日常 #希特勒 #绝育记`）→ 只有 `#希特勒` 那一格带头像，**其余两格样式不变**，且**位置/顺序完全不动**
- [ ] 一条推文里同时写真实名话题和别名话题、且指向同一只猫（如 `#希特勒 #小希`，小希是希特勒的别名）→ **两格都带那只猫的头像**（各带各的，不合并、不删格）
- [ ] 猫话题的缩略图加载失败 → 逐级降级（.png → 0.jpg → 占位图），不会出现永久空白
- [ ] 话题多、猫名长时：胶囊内文字出省略号（`#` 和头像都还在），不撑破卡片
- [ ] **点话题的行为未变**：命中猫 → 跳第一只猫的详情；没命中 → 搜同话题文章
- [ ] **长按话题的行为未变**：管理员长按猫话题 → 进那只猫的编辑页；长按普通话题 → 无反应
- [ ] 回收站预览（recoverMode）下同样按这套规则渲染
- [ ] 猫话题的查询失败（断网）→ 整行按普通话题渲染（不出现空头像、不整行消失），正文 / 评论不受影响
- [ ] 打开推文时不会看到整行"先普通、再跳成猫款"以外的抖动（两种胶囊文字与位置一致）

**评论**
- [ ] 有发布权限的用户可输入评论并提交
- [ ] 未获批 / 未注册用户看到的是灰掉不可输入的输入框（占位分别为「还没有发布权限，点「我的」申请」/「注册后再评论吧」，详见第六节）
- [ ] 评论列表正常显示（**所有人**都能看到全部评论，与发布权限无关）
- [ ] 空评论时显示"快来评论呀"占位
- [ ] 长按评论可删除（管理员/作者本人）
- [ ] 点击评论者头像预览大图

**回收站预览**
- [ ] 回收站进入时显示 `recover-bar` 提示
- [ ] 点击"编辑/恢复"跳转回收站编辑页

**已修复的 Bug 验证**
- [ ] 轮播图 `current` 正确切换（不再使用无效的 `parseInt`）
- [ ] 长按编辑时 `data-authorid` 正确传递（不再引用错误的 `item`）

### 5. addBooklet（发布推文）
**权限**
- [ ] 无发布权限（未注册 / 已注册未获批）用户进入 → 提示「无发布权限」+ 自动返回（已加权限门）
- [ ] 未注册用户进入 → 弹注册引导 + 自动返回
- [ ] 有发布权限（管理员 / 申请已获批）的用户正常进入

**表单**
- [ ] 标题必填校验（空 → 提示）
- [ ] 正文必填校验（空 → 提示）
- [ ] 话题必填校验（空 → 提示）
- [ ] 标题限长 30 字校验
- [ ] 正文限长 2000 字校验（textarea maxlength 已改为 2000）
- [ ] 话题限长 100 字校验
- [ ] 拍摄时间选择器
- [ ] 标题重名校验（已存在 → 提示）

**图片**
- [ ] 选图（相册/拍摄）正常
- [ ] 最多 20 张，达到上限拦截
- [ ] 长按拖拽排序
- [ ] 单击预览
- [ ] 删除确认弹窗 + "下次不再询问"
- [ ] 设为封面（移到第一位）

**发布**
- [ ] 图片上传到 COS 成功
- [ ] 草稿图/存档图走服务端复制（不重新上传）
- [ ] 发布成功后清草稿 + 跳转 index（首页）
- [ ] 限频：5 秒内只能发布一次
- [ ] 同时点击多次发布只生效一次（`_submitting` 锁）

**草稿**
- [ ] 编辑中切后台 → 自动保存草稿
- [ ] 返回上一页 → 自动保存草稿
- [ ] 重新进入 → 弹窗询问是否恢复草稿
- [ ] 确认恢复 → 表单和图片恢复
- [ ] 放弃恢复 → 草稿清除
- [ ] 发布成功后草稿清除

### 6. editBooklet（编辑推文）
- [ ] 加载已有推文数据（标题、正文、话题、图片）
- [ ] 修改后保存 → 更新 Page 集合
- [ ] 图片区交互同 addBooklet
- [ ] 草稿自动保存/恢复同 addBooklet
- [ ] textarea maxlength 已改为 2000

### 7. addCat（新增猫咪）
**表单**
- [ ] 所有必填字段校验
- [ ] 名称去空格
- [ ] 名称重名校验
- [ ] 下拉选择器（性别、状态、绝育、毛色等）值正确
- [ ] 照片上传到 COS（目录 main/images/cat/）

**关系**
- [ ] 选择相关猫咪 → 选择关系类型 → 双向建立关系
- [ ] 性别敏感配对自动建议

**提交**
- [ ] 写入 BITZH 集合成功
- [ ] 同步更新相关猫咪的 relatedCats 字段

### 8. editCat（编辑猫咪）
**加载**
- [ ] 加载已有数据；下拉选择器回填正确
- [ ] 照片列表正确显示

**修改**
- [ ] 修改后保存 → 更新 BITZH 集合
- [ ] 照片增删 → photoVer 自增（强制刷新缓存）
- [ ] 名称修改 → COS 上旧照片迁移到新名称
- [ ] 关系修改 → 双向同步

**⚠️ 已知风险**
- [ ] ~~共享对象引用问题~~（编辑时修改了对象可能影响缓存）

### 9. 所有（分类列表/"在线查猫"）
- [ ] 按分类过滤猫咪列表（从 index 页 navigator 传入 classification 参数）
- [ ] 瀑布流布局（左右列）
- [ ] 上拉触底分页加载
- [ ] 点击猫咪跳转 catDetail
- [ ] 管理员长按可编辑
- [ ] 缩略图加载失败 → 回退到占位图
- [ ] 空结果时显示空状态

### 10. regist（注册/修改资料）
**新用户注册**
- [ ] 昵称实时查重（防抖 300ms）
- [ ] 昵称危险字符清洗（sanitizeFileName）
- [ ] 头像选择（微信头像选择按钮）
- [ ] 手机号选填 + 格式校验
- [ ] 提交 → 上传头像到 COS → 写入 Feeder 集合
- [ ] 成功后更新 globalData + 跳转 mydetail

**已注册用户修改**
- [ ] 回填原昵称、头像
- [ ] 修改昵称 → 更新 Feeder + 同步历史推文作者信息
- [ ] 改名 → 删除旧头像 + 上传新头像

**⚠️ 已修复 Bug 验证**
- [ ] 头像上传失败时 spinner 能正常消失（已加 try-catch + hideLoading）
- [ ] updateFeeder/registFeeder 异常不会卡死 loading

**边界**
- [ ] 审核关闭时（audit=false）注册不可用 —— **但已注册（`Feeder.enable=true`）的用户不受影响**，仍能进本页改资料（见 §D）
- [ ] 限频：3 秒内只能提交一次

### 11. mydetail（我的 — 个人中心）
**身份**
- [ ] 未注册用户显示注册入口
- [ ] 已注册用户显示昵称、头像
- [ ] 已注册未获批用户显示「申请发送帖子」按钮；已获批 / 管理员则不显示（详见第六节）
- [ ] 管理员显示"后台管理"入口

**分栏**
- [ ] "历史"tab：显示我发布的帖子
- [ ] "回收站"tab：显示我删除的猫咪/推文
- [ ] 黑名单弹窗（popupAnimation 已初始化）

**回收站**
- [ ] 猫咪恢复：恢复到 BITZH 集合
- [ ] 推文恢复：恢复到 Page 集合
- [ ] 同名冲突时提示
- [ ] 恢复后列表刷新

### 12. about（关于页）
- [ ] 复制微信 / 复制电话 / 复制邮箱 / 复制公众号名
- [ ] 预览二维码大图
- [ ] 预览店铺图片
- [ ] 跳转合作小程序
- [ ] 黑名单弹窗（popupAnimation 已初始化）

### 13. Administrator（后台管理）
- [ ] 管理员身份校验（非管理员进入自动返回）
- [ ] 版块顺序：**操作密码 → 用户管理 → 审核与安全 → 内容管理 → 基础设置**（基础设置在最下面）
- [ ] 操作密码区：徽章在「未填写 / 已就绪」之间切换；点「填写密码」展开输入框，密码框是**隐藏字符**
- [ ] 审核开关：徽章 + 右侧**小圆按钮**（不是整行大按钮）；点它弹确认 → 开关翻转、徽章跟着变
- [ ] 联系方式卡片可展开/收起，展开后是手机号 + 邮箱两个输入框 +「保存联系方式」
- [ ] 公告管理入口 → editAnnouncement、官方推文入口、回收站二选一弹窗
- [ ] 所有入口能正常跳转（用户管理 / 管理员管理 / 发布申请 / 复核中心）

> ⚠️ 「审批投喂人（enable 字段切换）」这条**已作废**：投喂/发布权限现在走的是「发布权限申请-审批制」（`Feeder.canPost`），「审批投喂人」按钮已经不存在了。旧的黑名单管理指路 `BlackNum` 集合也已并入「用户管理」页，见 §七。

### 14. editAnnouncement（公告管理）
**⚠️ 已修复 Bug 验证**
- [ ] 新增公告：保存成功后弹窗才关闭（不再提前关闭）
- [ ] 修改公告：保存成功后弹窗才关闭
- [ ] 开关切换失败时回退到原状态
- [ ] 失败时显示错误 toast（不再静默）

**功能**
- [ ] 公告列表加载
- [ ] 新增公告（标题 + 内容 + 默认 status=true）
- [ ] 修改公告
- [ ] 开关控制公告展示/隐藏
- [ ] 限长校验（标题 30 字 / 正文 1000 字）

### 15. catTrash（猫咪回收站）
- [ ] 显示已删除的猫咪列表（BITZHdelete 集合）
- [ ] 空回收站显示占位（trash-empty）
- [ ] 恢复猫咪（恢复到 BITZH 集合 + 删 BITZHdelete 记录）
- [ ] 同名猫已存在时提示冲突
- [ ] 恢复后列表刷新

### 16. pageTrash（推文回收站）
- [ ] 显示已删除的推文列表（Delete 集合）
- [ ] 空回收站显示占位
- [ ] 恢复推文（恢复到 Page 集合 + 删 Delete 记录）
- [ ] 同名推文已存在时提示冲突

### 17. someBooklet（作者/标签推文列表）
**⚠️ 已修复 Bug 验证**
- [ ] 分享链接直接打开时 isFeeder/userId 正确初始化（已加 initUserState）

**功能**
- [ ] 按作者名查看：isName=true → 精确匹配 author 字段
- [ ] 按标签查看：isName=false → 模糊匹配 relative 字段
- [ ] 排序切换 + 分页加载
- [ ] 点赞
- [ ] 点击推文跳转详情
- [ ] 管理员/作者长按可编辑
- [ ] 参数错误时提示并返回（缺 name）

---

## 三、跨页面流程测试

### A. 用户注册全流程
- [ ] 打开小程序 → 首页正常
- [ ] 进入"我的" → 未注册 → 点击注册
- [ ] 填写昵称 → 查重提示 → 选头像 → 提交
- [ ] 注册成功后跳转 mydetail 显示资料
- [ ] 再次进入注册页 → 回填原有资料 → 可修改

### B. 发布推文全流程
- [ ] 已注册 → 小猫书 → 发布 → 填写标题/正文/话题 → 选图 → 发布
- [ ] 发布成功 → 跳转 index（首页）→ 新推文出现在列表
- [ ] 查看推文详情 → 评论 → 点赞
- [ ] 编辑推文 → 修改内容 → 保存
- [ ] 删除推文 → 进入回收站
- [ ] 回收站恢复推文 → 重新出现在列表

### C. 猫咪管理全流程（管理员）
- [ ] 新增猫咪 → 填写信息 + 上传照片
- [ ] 首页查猫 → 新猫咪出现在列表
- [ ] 猫咪详情 → 照片轮播正常
- [ ] 编辑猫咪 → 修改信息 → 保存
- [ ] 删除猫咪 → 进入回收站
- [ ] 回收站恢复猫咪 → 重新出现在列表

### D. 分享流程
- [ ] 分享首页 → 朋友打开 → 显示指定 tab
- [ ] 分享猫咪详情 → 朋友打开 → 显示指定猫咪
- [ ] 分享推文详情 → 朋友打开 → 显示指定推文
- [ ] 分享 someBooklet → 朋友打开 → 显示指定作者/标签

### E. 异常恢复流程
- [ ] 编辑推文中强退小程序 → 重新进入 → 草稿恢复弹窗
- [ ] 选择恢复 → 内容和图片正常恢复
- [ ] 选择放弃 → 草稿清除 → 空白表单
- [ ] 发布过程中断网 → toast 提示（不卡死）

---

## 四、边界场景

### 空数据
- [ ] 首页无猫（新部署）→ 各 tab 显示空状态
- [ ] 小猫书无推文 → 空列表
- [ ] 推文无评论 → "快来评论呀"
- [ ] 回收站为空 → 空占位提示
- [ ] 搜索结果为空 → 不显示结果列表

### 极端输入
- [ ] 标题输入正则特殊字符 `$.*+?^` → 存储正常、搜索正常
- [ ] 正文输入 emoji → 存储正常
- [ ] 超长输入（标题 >30、正文 >2000）→ 校验拦截
- [ ] 图片选 0 张 → 提交拦截
- [ ] 图片选满 20 张 → 选图入口拦截

### 并发
- [ ] 快速点击发布按钮多次 → 只执行一次（`_submitting` 锁）
- [ ] 快速点赞多次 → 每次 +1（设计如此）
- [ ] 快速切换 tab → 不重复加载数据（`_loadedTabs` 标记）

### 权限
- [ ] 无发布权限的用户访问 addBooklet → 提示 + 返回（已加门）
- [ ] 非管理员访问 Administrator → 返回
- [ ] 非管理员长按猫咪/推文 → 不响应
- [ ] 非作者长按推文 → 不响

---

## 五、已知问题（本次修复验证）

| 问题 | 验证方法 |
|------|---------|
| catDetail null 检查绕过 | 访问不存在的猫咪 _id → 显示"未找到"后返回 |
| catDetail NaN guard | 旧数据 addPhotoNumber 为 NaN → 显示 logo 占位 |
| catDetail 空正则数据泄露 | 猫名为空时 getPage 不查询 |
| bookletDetail parseInt | 轮播图 current 切换正常 |
| bookletDetail data-authorid | 长按推文正确传递 authorId |
| addBooklet 权限门 | 未注册用户进入 → 提示 + 返回 |
| someBooklet initUserState | 分享链接直接打开 → isFeeder 正确 |
| index nickname null | 搜索时遇到无昵称数据 → 不崩溃 |
| regist spinner | 头像上传失败 → spinner 消失 + 错误提示 |
| editAnnouncement 操作失败 | 失败时 toast 提示 + 弹窗不提前关闭 |
| editAnnouncement 开关回退 | DB 失败时开关回退到原状态 |
| textarea maxlength | 正文可输入 2000 字（非 500） |
| popupAnimation 缺失 | 黑名单弹窗动画正常 |
| 普通用户拿不回发布权 | 已注册未获批用户申请 → 管理员通过 → 完全退出重进后「+」出现 |
| 拒绝误伤已有权限 | 拒绝一条已获批用户的新申请 → 他退出重进后仍有发布权 |
| 全局开关关掉仍有申请入口 | 关「发布开关」→「我的」页的「申请发送帖子」一起消失 |

---

## 六、发布权限申请-审批制

> 发布权限从「仅管理员」改成**申请-审批制**：`Feeder` 新增 `canPost` 字段（只由云函数写），
> 客户端统一用 `db.canPublish()` = `isAdministrator || canPost` 判断。
> **通过后必须完全退出小程序再重新进入才生效**（从"最近使用"里划掉，或右上角关闭；返回上一页、切到后台再回来都不算）。

### A. 「我的」页 —— 申请入口与三态

- [ ] 未注册账号进「我的」页 → 只有「注册用户资料」红按钮，**没有**「申请发送帖子」按钮
- [ ] 已注册但未获批的账号进「我的」页 → 出现「申请发送帖子」按钮，且**可点**（不是灰色）
- [ ] 点「申请发送帖子」→ 弹二次确认（标题「申请发布权限」，正文「提交后由管理员审核，通过即可发布帖子和评论。每天只能申请一次。」，确认键「提交申请」）→ 点「取消」→ 按钮仍是可点的「申请发送帖子」，**不消耗当天次数**（再点一次仍能弹出同一个确认框）
- [ ] 点「申请发送帖子」→ 点「提交申请」→ 弹告知弹窗「已提交」（正文含"通过后需要完全退出小程序再重新进入"、只有「知道了」一个键）→ 关闭后刷新「我的」页 → 按钮变成**禁用的**「已申请，等待审核」
- [ ] 同一天再申请 → 按钮已禁用点不动；也可在开发者工具 Console 直接调 `postApply` 云函数验证 → 返回「每天只能申请一次，请明天再试」

### B. 「发布申请」页 —— 审批队列（**已从「管理员管理」拆成独立页面**）

> 2026-09-16 拆页：「管理员管理」原来一个页面里塞了管理员名单 + 发布申请审批，「内容太多、超载了」。
> 现在审批在独立页 `pages/postApplyManage/`，入口是后台「用户管理」→「发布申请 / 审批 / 处理记录」。

- [ ] 管理员从「关于」页长按社团 logo 进后台 →「用户管理」→「管理员管理 / 添加 / 移除」→ **点进去只剩一张「发布申请（N）」入口卡**（N = 待审批条数，由 `onShow` 拉取），列表本身**不在这一页了**
- [ ] 点那张卡进「发布申请」页 → 若后台顶部已填过操作密码 → **不出现密码输入框**，直接显示待审批列表（这是"密码输一次、处处复用"的直接验证）
- [ ] 点某条的「通过」→ 弹「通过发布申请」+「通过「<昵称>」的发布申请？ 通过后对方永久获得发布帖子和评论的权限。」+「确认通过」→ 确认后弹「已通过」并附"完全退出重进"提示 → 该条从队列里消失、N 减 1
- [ ] 同页下半部「最近处理」：上一步刚批的那条**出现在这里**，带**处理人姓名**（`handledByName` 快照）和处理时间
- [ ] 上一步被通过的人**完全退出小程序再重新进入**（从"最近使用"里划掉，或右上角关闭）→ 底部「+」出现、能进发布页发帖、推文详情底部评论框可输入且有「发送」按钮（只返回上一页 / 切到后台再回来**不算**重新进入，此时仍看不到「+」）
- [ ] 「拒绝」：先让一个**已获批**的账号再申请一次 → 管理员点「拒绝」→ 弹「拒绝发布申请」+「拒绝「<昵称>」的发布申请？ 只会把这条申请标为已拒绝，不会收回对方已有的任何权限。对方今天不能再申请，明天起可以重新申请。」+「确认拒绝」→ 确认后只出现「已拒绝」轻提示（无弹窗）→ 该条从队列消失；该**已获批**账号完全退出重进后**仍有发布权**（底部「+」还在）—— 验证"拒绝不收回对方已有权限"
- [ ] 黑名单用户：先在用户管理里封禁某人 → 他进「我的」页点「申请发送帖子」→ 提示「当前账号无法申请发布权限」；若他已有一条待审批申请，管理员点「通过」→ 弹「该用户在黑名单中，不能授予发布权限」→ 他完全退出重进后**仍然没有**「+」和评论框

### C. 评论区 —— 三态输入栏

- [ ] 三种身份分别打开推文详情：**未注册** → 灰掉不可输入的输入框 + 占位「注册后再评论吧」；**已注册未获批** → 灰掉不可输入 + 占位「还没有发布权限，点「我的」申请」；**已获批** → 正常输入框 + 「发送」按钮（前两种灰框都**没有**「发送」按钮）；同时确认**未注册 / 未获批账号都能正常浏览全部评论**

### D. 全局审核开关 + 豁免（`Feeder.enable` / 管理员名册）

**【口径】** 豁免分两片，**判据不一样**（这是最容易搞混的地方）：

| 哪一片 | 涉及的地方 | 豁免判据 |
|---|---|---|
| **公共内容** | 首页瀑布流、评论区、底部加号 | `Feeder.enable === true`，或在 `BITZHAdministrator` 名册里 |
| **本人的数据** | 「我的」页（头像、昵称、历史、回收站、**「申请发小猫书」**）**与注册页整页** | **有 `Feeder` 记录就算数**（`isFeeder`），不看 `enable`；或在该名册里；或临时注册入口已开（见 D2-B） |

判断式见 `utils/auditGate.js`（`auditOpen` / `myPageOpen`），公共内容读 `db.getAuditForMe()`、「我的」页与注册页读 `db.getAuditForMyPage()`，**管理员页仍读全局 `db.getAudit()`**。

> **注册页为什么算"本人的数据"**：它唯一的入口是「我的」页的注册按钮/头像。两处判据若不一致，就会出现**「按钮看得见、点进去是白页」**——这在改用同一判据之前是真实存在的 bug（审核模式下已注册的人点自己头像，整页空白）。

**【`enable` 怎么才会变成 true】** 只有两条路，**注册本身不给**：
1. 管理员审批通过发布权申请（`adminManage` 的 `applyDecision`，与 `canPost` 同时写）；
2. 用户在「关于」页**连点 logo 五次**（已注册者）→ `postApply` 云函数的 `selfEnable`，另推一条通知到「发布申请」群。

> **未注册的人连点五次走的是另一条路**：只临时开放注册入口（内存态），不写库、不走云函数、无通知。详见 D2。

> 【怎么在开发者工具里造出"公共内容受限"的用户】你本人永远有多道豁免（是管理员 + 注册过 + 可能还有 `enable`），所以这个功能**在你自己手机上测不出来**。
> 两步**都要做**，只做第一步不管用：
> 1. 把 `Feeder` 那条记录的 `enable` 删掉或改成 `false`；
> 2. **再从 `BITZHAdministrator` 里把自己那条记录删掉或注释掉** —— 否则管理员豁免照样放行，你会误以为"改脏没生效"。
>
> 更省事的做法：换一个**没加进名册**的微信号登录。测完记得改回来。

- [ ] 管理员关掉「发布开关」→ 用 **`enable=true`** 的账号看：首页瀑布流**照常有内容**、推文详情**有评论区**、底部「+」**仍在**、「我的」页照常显示
- [ ] 同一开关下 → 用**未注册 / 缺 `enable` / `enable=false`** 的账号看：首页**一条推文都没有**、进详情页**没有评论区**、底部「+」消失（管理员仍能看到「发官方推文」）
- [ ] **公共内容判据是 `enable`，不是"注册过没有"** —— 一个注册了但没 `enable` 的人，首页必须是空的。这一格最容易写错（因为「我的」页恰好反过来）
- [ ] **「我的」页判据反过来**：同一个"注册了但没 `enable`"的账号，进「我的」页必须**看到头像、昵称和历史**，并且**看得到「申请发小猫书」按钮** —— 这正是本轮要的效果
- [ ] **「我的」页对陌生人仍然关闭**：未注册的账号进「我的」页 → 审核模式下整块空白（连「注册用户资料」按钮都没有）
- [ ] **注册页与「我的」页口径一致（白页回归）**：审核模式下用一个"已注册但没有 `enable`"的账号 → 进「我的」页点头像 → 注册页必须**显示表单**，不能是整页空白。（以前注册页用的是 `getAuditForMe()`，与「我的」页口径不同，会白页；两处现在共用 `db.getAuditForMyPage()`）
- [ ] **管理员豁免**：造一个"只在 `BITZHAdministrator` 名册里、`Feeder` 里没有记录"的账号 → 审核模式下**照常有内容、有评论区、有加号**，且「我的」页也正常
- [ ] 管理员不会被锁在外面：`Administrator` 页的入口在 about 页，只判断 `isAdministrator`、与 `audit` 无关 → 审核模式下**始终进得去**，开关随时关得掉
- [ ] **管理员页显示的是全局值**：管理员打开「管理员」页，审核模式开着时那个开关必须显示**「审核中」**。若显示「开放中」，说明误用了 `getAuditForMe()` / `getAuditForMyPage()` —— 它们对管理员**永远**返回 `true`
- [ ] 开关切回 `true` → 上面那个受限账号**立刻恢复**（首页有内容、有评论区），且期间**没有写坏任何数据**（受限只是显示层）
- [ ] 下拉刷新也走豁免：公共内容受限的账号下拉**不会**意外拉出内容；`enable=true` 的账号下拉**照常**刷新
- [ ] 单测钉住判断式：`node tests/auditGate.test.js`（61 项，两套真值表各 8 格 + 临时注册入口表 + 两组反向断言：「两个判断式必须不同」「临时入口不许漏进公共内容」）

### D2. 「关于」页连点五次 → 按「注册没注册」分成两条路

**入口**：「关于」页**顶部 logo**，**连点 5 次**（同一次停留内累计，切走页面清零）。
`2026-09-17` 改的：原先挂在最底部那四行上，现在那四行**只是署名、点了没反应**。

> 两条路的判据是 `app.globalData.isFeeder`（`about.js` 的 `staffTap`）。**同一个手势、两种后果**，
> 所以必须**两种身份各过一遍** —— 只测已注册的那个号，B 组完全覆盖不到。

**A. 已注册账号 → 开通审核模式豁免（`Feeder.enable = true`）**

- [ ] 连点 5 次 → 弹窗「已开通」，正文两句：「审核模式下现在可以看见内容了。」+「若首页还是进不去，请完全退出小程序后重新进入。」
- [ ] 点「去首页」跳到首页 → **瀑布流当场就出来**，**并且跳转不卡顿**。这两件事是同一个改动（`db.markSelfEnable` 只写 `state.enable`，不再清 `userId` / 名册 / 全局开关）：清了那三个的话，首页闸门要连着跑 **4 次网络往返**才给得出答案 —— 表现就是"切过去要愣一下"，而且**首页的瀑布流只在 onLoad 读一次闸门**，切回来不会自己重判（现在由 index 的 `onShow` 补算）。第二句提示只是兜底，正常**不必**真去重进
- [ ] ⚠️ **长按 logo 与连点不冲突**：管理员长按 logo → 进「管理员」页，**且这次长按不占连点计数**（长按后回来再连点 5 次仍应触发）
- [ ] ⚠️ **底部四行不再触发**：在「小程序开发 / 信息整理 / logo制作 / v6.6.6」那四行上连点 5 次 → **什么都不该发生**（挪入口时最容易漏掉的一处）
- [ ] 点第 1~4 次**什么都不该发生**（没有提示、没有报错）
- [ ] **写库走的是云函数**：`Feeder.enable` 变 `true`，**且 `canPost` / `canPostBy` / `mutePost` 一个都没被动过**
- [ ] **飞书通知**：「发布申请」群里出现一条「【自助开通】审核模式豁免」，带**用户ID**、昵称、时间，并写明「无需回复」
- [ ] 🔴 **通知卡不能被当成申请卡**：在**那条通知**上回一句「同意」→ 必须被拒（提示只能在【发布申请】推送下回复），**绝不能批出发布权**。两张卡片发在同一个群里、长得很像，这是本次最要紧的负例
- [ ] 已经是开通状态再连点 5 次 → 提示「已经是开通状态」，且飞书群**不会再收到第二条通知**
- [ ] **它不给发帖权**：连点 5 次之后，底部「+」**仍然不出现**（加号要 `canPost` 或管理员）；去「我的」页看，「申请发送帖子」**仍在**。这是"自助 ≠ 审批"的直接验证
- [ ] 切到别的 tab 再回来 → 计数清零（连点 3 次 +切走+ 回来 + 连点 2 次 **不应**触发）
- [ ] 通过审批拿权限的人 → 审批后 `Feeder.enable` 也是 `true`（`adminManage` 与 `canPost` 同时写），但**「我的」页里申请按钮是否消失只取决于 `canPost`**，与 `enable` 无关

**B. 未注册账号 → 临时开放注册入口（不写库 / 不走云函数 / 无通知）**

- [ ] 连点 5 次 → 弹**弹窗**「已开放注册入口」（不是一个一闪而过的 toast），点「去注册」跳到「我的」页
- [ ] 「我的」页里**出现「注册用户资料」按钮**（审核模式下平时没有）→ 点进去**注册页有表单**（不是白页）
- [ ] 🔴 **它不开内容**：这个未注册账号回首页 → 瀑布流**仍然一条推文都没有**、进详情**没有评论区**、底部「+」**仍然不在**。这是本次最危险的写法（临时入口一旦漏进 `auditOpen` 就等于关掉审核模式），必须亲手验一遍
- [ ] **「暂时」是真的**：杀掉小程序重进 → 「我的」页的注册按钮**又消失了**（它只在内存里，没落 storage、没写库）
- [ ] 中途切到别的 tab 再回来 → 注册按钮**还在**（内存态在该次运行内一直有效，切页面不该清掉它）
- [ ] 注册成功后再看：「我的」页正常显示头像昵称（此后靠 `isFeeder` 兜着，临时入口是否失效都无所谓）
- [ ] **没有飞书通知**：整个过程「发布申请」群里**不该**出现任何消息（与 A 组那条通知对照着看）

> ⚠️ **这个口子会让审核模式挡不住"知道内情的人"**：连点五次是自助的，手法就写在小程序自己的「关于」页上。它挡的是碰巧路过的游客，不是知道手法的微信审核员。如果需要真正挡住后者，得去掉这个自助入口（或加一道口令）。

### E. 本地回归

- [ ] 跑 `node tests/run-all.js` —— 13 个脚本全绿、退出码 0（合计 842 项）。它会把失败的脚本单独印出完整输出

---

## 七、后台管理页（2026-09-16 三处改动）

> 本轮改的三块：**发布申请拆页**（见 §六.B）、**「管理员」页重排 + 操作密码**、
> **动态敏感词库 + 禁言**。这几页原先都不在本清单里，一并补上。

### A. 操作密码 —— 「填一次、处处复用」的内存语义

- [ ] 后台顶部填密码 → 点「验证并保存」→ 徽章变「已就绪」+ 出现"已记住，本次使用期间通用…只存在内存里"说明
- [ ] 有意输错一次 → 提示「密码不对（多次错误会被暂时锁定）」—— **不能**是"已保存"
- [ ] 连错 5 次 → 弹「已暂时锁定」（不是 toast）→ 之后即使输对也仍然被拒（按 IP + 用户 ID 锁 10 分钟）
  - ⚠️ 锁定期内**填了密码但什么都没做**（比如只是打开「用户管理」页），**不该**消耗失败次数；否则几次误触就把管理员锁了
- [ ] 密码就绪后进「管理员管理 / 发布申请」→ **都不再要密码**，且「管理员管理」页的解锁卡**自动隐藏**
- [ ] **杀掉小程序重进**（从"最近使用"里划掉或右上角关闭）→ 又要重新输密码（证明只存内存、没落 `storage` / `globalData`）
- [ ] 后台顶部**不填**密码也能用：进上述页面会现问你一次

### B. 动态敏感词库 —— 加词不重新部署即生效

> 【这组测试为什么重要】词库是唯一一条"不重新部署就能改变全站行为"的路径，
> 而且**读不到时是 fail-open（静默失效）** —— 集合不存在时页面一切正常，只是加了词不生效。

- [ ] 后台「审核与安全」→「敏感词库」→ 展开：显示词数、输入框、两个档位卡片（**拦下** / **只标记**）、「加入词库」
- [ ] 加一个**自己编的、不可能有人用的词**（如 `测试词abc`）→ 档位选「拦下」→ 弹确认（写明档位后果）→ 确认后列表里出现它
  - ⚠️ **别拿真词试**：加进 block 是**全站硬拦**，会影响所有人发帖
- [ ] 用另一个账号（或自己）发一条**含该词**的帖子 → **被拦下**（证明动态词库生效、无需重新部署）
- [ ] 删掉该词 → 同一条内容**能发出去**（证明删除入口有效 —— 这是手滑加错词时唯一的补救路径）
- [ ] 加同一个词两次 → 不产生重复行；第二次提示「**本来就在词库里，档位就是「拦下」，没有改动**」（不能说"已把档位改为…"，那会让人以为哪里出了问题）
- [ ] 对同一个词改档位（先「拦下」再加一次选「只标记」）→ 第二次提示「本来就在词库里，**已把档位改为「只标记」**」
- [ ] 档位选「只标记」加一个词 → 含它的内容**照常发布**，但管理员在复核中心能看到一条待复核
- [ ] 输入 1 个字 / 21 个字 → 分别被前端拦下（提示至少 2 / 最多 20 个字），**不发请求**

### C. 推荐词表导入（`seedWords`）

- [ ] 词库区点「导入推荐词表（幂等，只补缺的）」→ 弹确认 → 确认后提示「已导入 43 条：新增 N、改档 0、本来就有 0」
- [ ] **再点一次** → 提示变成「新增 0、本来就有 43」—— **列表里不出现重复词**（幂等）
- [ ] 导入后抽查：`nuemao` 在列表里、档位「拦下」；`猫奴` 在列表里、档位「只标记」
- [ ] **不填操作密码**点导入 → 提示「需要操作密码：请在上方「操作密码」里填写」，**词库列表不变**
- [ ] 【关键探针】导入后去控制台「云函数 → `secCheck`」用调试面板传 `{"action":"reloadWords"}` → 返回的 `extraWords` **应当 > 0**，且 `bankState` 是 `"ok"`：
  - `extraWords: 43` + `bankState: "ok"` → 动态词库正常
  - `extraWords: 0` + `bankState: "ok"` → **集合读通了但一条词都没有**，多半是还没导入过（或集合名少个字母、建错地方了）
  - `bankState: "read_failed"` → 读的时候报错了，查集合在不在、名字是否一字不差
  - `bankState: "no_db"` → 云函数没拿到数据库句柄（平台侧异常，不是词库的问题）
  - 返回里还有一句 `bankHint`，就是上面几句人话，直接照它做
  - 线上有多个容器、缓存各自计 60 秒，所以小程序里"加完词立刻试"可能要等一分钟；这个 action 会顺手清掉本容器的缓存

### D. 禁言（剥夺发帖权，**不碰已发内容**）

- [ ] 后台「用户管理」搜到某人 → 状态标签出现第三态：**正常 / 禁言中（琥珀色）/ 已拉黑（红色）**
- [ ] 点「禁言」→ 弹**可编辑**的原因输入框 → 填原因确认 → 提示「已禁言」→ 该行标签变「禁言中」、按钮变「解除禁言」
- [ ] 禁言后紧接着弹「要加入词库吗？」的可**跳过**弹窗：
  - [ ] 填一个词提交 → 去「敏感词库」看，词在列表里
  - [ ] 留空直接确认 → **不报错**，禁言仍然已生效（"不回答也继续禁言"）
  - [ ] 后台没填过操作密码时填词 → 提示「关键词未入库：请先在「管理员」页填写操作密码」，**禁言不受影响**
- [ ] 被禁言者**完全退出小程序再重进** → 底部「+」消失、发不了帖、推文详情评论框灰掉；**但自己的旧帖、旧评论原样都在**（这是本功能唯一容易写错的地方：禁言绝不能碰 `Page` / `Comment`）
- [ ] 被禁言者**仍能**浏览、点赞、看别人的帖子（禁言 ≠ 封禁）
- [ ] **管理员自己被禁言** → 同样发不了帖（`canPublish` 里 mute 条件放在最后，覆盖管理员）；而**另一个管理员**能解除
- [ ] 解除禁言 → 对方完全退出重进 → 「+」和评论框回来；若他本来就没发布权（未获批），解除后**仍然发不了**（说明 `mutePost` 与 `canPost` 正交，解除禁言不会凭空授予权限）
- [ ] **不许禁言自己**：管理员禁言自己 → 服务端拒绝（`SELF_MUTE`），页面**不能**提示「已禁言」（这条靠 `ensureModOk` 保证：云函数 `ok:false` 必须被抛出来，不能被静默吞掉）

### D2. 撤销「审核模式豁免」（飞书通道，**连点五次也开不回来**）

> 【这组为什么必须人工过一遍】单测全是 mock，而这条链路的三个环节**跨两个云函数 + 一张飞书卡片**：
> 卡片首行 → `detectContext` 判场景 → 解析器抠 ID → `adminManage.revokeEnable` 写库 → 本人连点五次被 `postApply.selfEnable` 拦住。
> 其中**任何一环断了，表现都是"什么都没发生"**：撤销是粘性的、撤错人当场看不出来。
> **前置**：本轮已上传 `adminManage` / `postApply` / `feishuCallback` 三个包，且 `FEISHU_INTERNAL_SECRET` 已配（没配的话回执会说"触发失败"，见最后一条）。
> ⚠️ **2026-09-17 起没有「恢复豁免」这条命令了**，所以下面"恢复"那一节的验证方式换成了**走审批**（那也是现在唯一的解锁路径）。

**准备**：拿一个**没有发帖权、但已在审核模式下连点五次开通过豁免**的账号（下称"小白鼠"，记下它的 **24 位 `用户ID`**）。
- [ ] 先确认它的 `Feeder` 记录里 `enable: true`（控制台或后台都能看）

**撤销**
- [ ] 在「发布申请」群找到那张 **【自助开通】审核模式豁免** 卡片（连点五次时推的）
- [ ] 在**那张卡片下**回复「**撤销豁免**」→ 回执「✅ 已收回「<昵称>」的审核模式豁免。…（已上锁）」**并报出昵称**（报昵称是为了让你当场发现"撤的是不是这个人"）
- [ ] 小白鼠**完全退出小程序再重新进入** → 审核模式下**看不见首页内容**、评论区不可见
- [ ] 🔴 **本组最关键的一条**：小白鼠在「关于」页**连点 logo 五次** → 提示「**该账号的审核模式豁免已被管理员收回，如需恢复请在「我的」页重新申请发布权限**」，**且连点后仍然看不见内容**（这条就是"上锁"的全部意义；没有锁的话他一点就复原了，而且你**不会收到任何通知**）
- [ ] 控制台看那条 `Feeder`：`enable: false`、`enableRevoked: true`、`enableRevokedBy` 是**发命令那个人的 `open_id`**、`enableRevokedTime` 是刚才
- [ ] ⚠️ 确认 `canPost` **没有被改动**（撤销豁免 ≠ 撤销发帖权）

**恢复（唯一路径 = 让对方重新申请，**不是**飞书命令）**
- [ ] 在同一个群回「**恢复豁免**」→ **应该是「未识别」提示**（这条命令已删；若仍回「✅ 已恢复…」说明 `feishuCallback` 那个包没传新的）
- [ ] 小白鼠去「我的」页重新提交发布权申请 → 管理员在申请卡片上回「**同意**」→ 回执报已批准
- [ ] 小白鼠完全退出重进 → 审核模式下**又能看见内容**；再连点五次 → 回到「**你已经是开通状态，无需重复操作**」（锁确实被审批清掉了）
- [ ] 回「**撤销豁免**」**再**发一次 → 仍是「✅ 已收回」（幂等，不报错）

**逃生口与边界（每条都是防止"撤错人"）**
- [ ] 「**撤销豁免`<用户ID>`**」带 ID 的形式 → 直接生效，**不依赖父消息**
- [ ] 🔴 **【待复核】推送下回「撤销豁免」必须被拒** → 回执提示"只能在【自助开通】通知下回复"。**去确认被复核的那位作者毫发无伤**
  - （`secCheck` 的推送里也有「用户ID：」行，这就是这条守卫存在的原因 —— 撤错人是粘性的，且撤错当时没有任何提示）
- [ ] **【举报】推送下**回「撤销豁免」→ 同样被拒；而**同一张卡片**下回「禁言用户」**仍然照常可用**（宽窄是分开定的，别把既有命令一起改窄了）
- [ ] **发布申请卡片**下回「撤销豁免」→ 被拒；同一张卡片下「**同意**」**照常有效**（本功能没动授权那条线）
- [ ] 在【自助开通】卡片下回「**同意**」→ **被拒**（它只给豁免、不给发帖权，首行守卫没被放宽）
- [ ] 回「**撤销豁免了**」/「**撤销权限**」/「**封禁豁免**」/「**恢复豁免**」→ 都**不该**被认成这条命令
- [ ] 随便发一个「喵」→ 「未识别」提示里**能看到新命令**（否则管理员永远不知道有这功能）

**跨函数契约的反向验证（撤错了怎么救）**
- [ ] 撤销某人 → 让他**去申请发布权** → 管理员在申请卡片上回「同意」→ 提示已批准
- [ ] 他完全退出重进 → 既有「+」**又看得见内容**（说明审批**顺手清掉了锁**，否则"误撤销"就只剩"去控制台改库"一条路，那正是本功能想避免的误操作反过来咬人）

### E. 飞书命令（需先部署 `moderate`）

> ⚠️ 本轮给 `moderate` 加了 `mute` / `unmute`，**不重新上传 `moderate` 的话**，飞书里发「禁言用户」会拿到「未知动作」。

- [ ] 飞书群里回复推送「**禁言用户**」→ 评论区回执「✅ 已禁言」
- [ ] 「**解除禁言**」→ 回执「✅ 已解除禁言」
- [ ] 「**禁言用户 `<id>`**」/「**解除禁言 `<id>`**」→ 逃生口可用
- [ ] 回「**禁言用户名**」（多打了字）之类的**不该**被误判成禁言
- [ ] 随便发一个字（如「喵」）→ 回执里的「未识别」提示**能看到新命令**（否则管理员永远不知道有这功能）

### F. 本轮部署顺序（硬约束，顺序颠倒会白忙）

1. `postApply-deploy.zip`（新函数）
2. `adminManage-deploy.zip` —— **三个文件一起**（`index.js` + `config.js` + `package.json`），少 `config.js` 会丢 `ADMIN_PASSWORD`
3. `feishuCallback-deploy.zip`
4. `secCheck-deploy.zip`
5. 小程序包（开发者工具「上传」）
6. `moderate-deploy.zip` —— **本轮必须传**（禁言在里面）
7. 飞书新群：拉入机器人 + 开「接收所有消息」，验证回执带本群 `chat_id`
8. 新建集合 `SensitiveWord`（动态词库用，见 README 第五节；**不建则 §七.B/C 全部表现成"加了词不生效"**）

### G. 三个安全负例（一个都不能过）

- [ ] **主群回「同意」必须被拒**：在**主群**（非申请通知群）回「同意」→ 不授予任何发布权，回执提示场景不对
- [ ] **黑名单必须否决**：封禁某人后再批准他的发布申请 → 弹「该用户在黑名单中，不能授予发布权限」，对方完全退出重进后**仍然没有**「+」
- [ ] **同一天第二次申请必须被拒**：同一天再申请 → 「每天只能申请一次，请明天再试」（`PostApply._id` = `<userId>_<北京日期>` 是原子锁）
